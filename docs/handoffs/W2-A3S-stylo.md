# W2-A3S standalone Stylo handoff

- Task: W2-A3S admit the imported Stylo Rust core behind a Linux-only Wild Buzzard platform profile
- Owner: Agent 3 — DOM, style, and layout; corrected and independently audited before main-orchestrator integration
- Status: Complete for the standalone CSS-engine workspace; this is not a live-DOM, layout, browser, complete-CSS, or Firefox-parity claim
- Firefox commit and source paths: ESR153 `c19b7e89270787889495688244ec6ee8e79288a1`; the eight imported `servo/components` groups and exact `xpcom/rust/malloc_size_of_derive` source are recorded in `docs/upstream-components.toml`; historical standalone Servo boundaries are recorded in `servo/WILDBUZZARD_ADAPTATION.md`
- Firefox test paths: the focused cascade, CSSOM, selector, and Web Platform Test references inspected for this slice are enumerated in `servo/WILDBUZZARD_ADAPTATION.md`
- Wild Buzzard paths changed: `servo/Cargo.toml`, `servo/Cargo.lock`, `servo/.cargo/config.toml`, imported/adapted Stylo crates under `servo/components`, four `wild_buzzard_*` shim crates, focused style tests, pinned Python build requirements, and the adaptation record
- Contract added or changed: an exact-target nested workspace; default `wild_buzzard` style profile; static atom and namespace domains; exact ESR document/element state bits; typed compile-time release preferences; local Linux thread/profiler hooks; real generated property parsing/cascade/computed values; ESR-facing `:has()` and `:nth-child(... of ...)` parser policy; fail-closed stylesheet-URL shared-memory transfer
- Tests run and results: 44 tests passed and 3 imported documentation examples remained ignored; 18 passing tests focus on generated properties/CSSOM, actual product-parser policy, generic matching, real no-DOM cascade depth, exact-target/platform contracts, atoms/preferences, and the shared-memory boundary; all-target check, release build, focused warning-denied Clippy and rustdoc, full workspace rustdoc with four documented imported-debt allowances, and expected Gecko-feature rejection passed using external build trees
- Parity evidence: the normal build executes the pinned Mako generator and compiles 107,660 generated Rust lines representing 256 longhands and 69 shorthands; focused tests exercise declaration parsing, serialization, priorities, custom properties, generated cascade, inheritance, logical mapping, viewport units, selectors, matching, and malformed-input rejection
- Known behavioral differences: there is no concrete `TNode`/`TElement`/`TDocument`/`TShadowRoot` implementation over the Wild Buzzard DOM, no revision-matched owned computed-style snapshot, no live invalidation, and no layout connection; the generated property universe is the pinned Servo profile rather than Gecko product parity; normal line-height, scrollbar size, effective dark scheme, preferred scheme/look-and-feel values, and traversal statistics still have explicitly recorded fallback behavior that must not feed product layout evidence
- Unsafe or FFI introduced: every new Wild Buzzard shim forbids unsafe code; one focused test uses the unsafe shared-memory-builder constructor with a bounded live array and documented invariant; imported `servo_arc`, `to_shmem`, shared-lock, rule-tree, generated-property, and allocation code retain audited unsafe internals; no active bindgen, clang, C++, XPCOM, `nsstring`, `mozbuild`, or Gecko profiler boundary is present
- Licenses and provenance: imported Stylo/support source retains MPL-2.0 notices; the exact `malloc_size_of_derive` import retains its MIT and Apache-2.0 files; first-party adaptation source is MPL-2.0; provenance and local patches are recorded in `docs/upstream-components.toml`
- Provider or network implications: None; property generation and runtime perform no network, telemetry, provider-service, profile, or credential operation, and the only shim URL strings are standards namespaces or `.invalid` tests
- Blocked on: a safe immutable DOM trait adapter, owned revision-checked computed styles, real font/device/theme metrics, UA and user stylesheets, full CSS/WPT/reftest coverage, and the static-page vertical integration
- Recommended next action: implement the concrete immutable Wild Buzzard DOM-to-Stylo adapter and computed-style snapshot, replace the correctness-blocking device/font fallbacks, and then feed the accepted layout/display-list/WebRender path without weakening target, generation, or fail-closed gates
