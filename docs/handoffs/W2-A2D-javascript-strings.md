# W2-A2D JavaScript string and property-key handoff

- Task: W2-A2D exact ECMAScript UTF-16/WTF-16 string representation and string-key semantics
- Owner: Agent 2 — JavaScript/WebAssembly; corrected, integrated, and independently reviewed by the main orchestrator
- Status: Complete for the bounded Wave 2 subset; this is not full ECMAScript, SpiderMonkey, DOM-binding, WebAssembly, browser, or Firefox parity
- Firefox commit and source paths: ESR153 `c19b7e89270787889495688244ec6ee8e79288a1`; string representation, text utilities, public string/id APIs, property keys, tokenization, primitive string access, and DOM string-boundary paths plus relevant history revisions are recorded in `js/README.md`
- Firefox test paths: focused Test262 and SpiderMonkey string source-text, Unicode escape, ordering, numeric-property, code-unit, well-formedness, UTF-8 boundary, and maximum-length cases are listed in `js/README.md`
- Wild Buzzard paths changed: `js/src/string.rs`, `js/src/{ast,heap,lexer,lib,parser,runtime}.rs`, `js/tests/{embedding,gc,object_model,semantics}.rs`, and `js/README.md`
- Contract added or changed: public immutable `JsString` owns exact `Arc<[u16]>` contents through the ESR153 maximum `(1 << 30) - 2`; checked UTF-8 and exact-code-unit constructors are distinct from explicitly lossy diagnostics; values remain generation-checked heap records while object keys own immutable string content; embedding property APIs accept exact keys; public function names and caught host-error messages validate length fallibly
- Tests run and results: final JavaScript suite passed 87 tests (29 unit, 9 embedding, 14 GC, 16 object-model, and 19 semantic); direct-file Rust 2024 formatting, package all-target/all-feature check, strict Clippy with `-D warnings`, locked tests, release build, warning-denied rustdoc, root workspace all-target check, and root locked tests passed for `x86_64-unknown-linux-gnu` using external targets
- Parity evidence: literals preserve raw supplementary scalars, fixed/braced escapes, lone surrogates, embedded NUL, and cross-concatenation surrogate pairs; LF/CR/CRLF/U+2028/U+2029 continuations, source lines, ASI, and single-line comment termination are handled explicitly; malformed, out-of-range, and unsupported legacy decimal/octal escapes fail closed; equality, hashing, ordering, concatenation, length, indexing, primitive deletion, numeric conversion of ill-formed strings, exact property keys, and canonical Array-index endpoints have focused regressions; a property key survives collection of the string value from which it originated
- Known behavioral differences: strict-mode directives are absent, so primitive string assignment currently implements only the non-strict ignored-write path; symbols, complete object ToPrimitive/ToPropertyKey, exact number formatting/StringNumericLiteral whitespace, legacy decimal/octal escapes, templates, regular expressions, iterators, normalization/well-formedness intrinsics, ropes/dependent/inline/Latin-1/external/atom/shared representations, ordered own-key enumeration, byte budgets, and OOM-safe allocation remain pending
- Unsafe or FFI introduced: None; the crate forbids unsafe code, adds no native engine wrapper, and has no external dependency
- Licenses and provenance: MPL-2.0 first-party Rust informed by the pinned ESR153 implementation, tests, and history; no Firefox source was copied and `firefox/` is not a build or runtime input
- Provider or network implications: None; the crate performs no I/O and introduces no endpoint
- Blocked on: Nothing for continued runtime work; generated DOM bindings will additionally require explicit DOMString/USVString/ByteString conversions and shared rooting rules
- Recommended next action: add Symbols plus ordered own-key enumeration and a pinned metadata-aware Test262 shard, while beginning a minimal WebAssembly decoder/validator that shares the existing rooted heap boundary
