# W2-A2G Brimstone baseline import handoff

- Task: Pin and import the newest selected Brimstone source as Wild Buzzard's canonical JavaScript execution-engine baseline, and decide the Wasmtime browser-Wasm direction.
- Owner: Agent 2 — JavaScript/WebAssembly; imported and independently verified by the main orchestrator.
- Status: Complete for the exact source-import boundary only. Brimstone is not yet root-integrated, production-safe, JIT-enabled, browser-hosted, or a Firefox/ECMAScript parity implementation.
- Firefox commit and source paths: ESR153 `c19b7e89270787889495688244ec6ee8e79288a1`; SpiderMonkey `js/src`, its GC/JIT/Wasm/runtime ownership boundaries, and tests remain behavioral and architecture reference. No SpiderMonkey source was copied into this independent upstream import.
- Firefox test paths: applicable SpiderMonkey regression behavior, in-tree Test262 metadata, Wasm specification tests, and DOM WebAssembly/WPT paths remain required as adaptation evidence; this import ran Brimstone's own pinned harness only.
- Wild Buzzard paths changed: exact upstream tree at `js/brimstone`, `js/brimstone/WILDBUZZARD_UPSTREAM.md`, `AGENTS.md`, `docs/architecture/javascript-wasm-runtime.md`, `docs/{component-map,import-status,program-status,upstream-components.toml}`, and this handoff.
- Contract added or changed: Brimstone commit `b544eff181ef6a72639f26a89b6aca1f8d6e6b50`, tree `9c2c7b675799b0a05aa11ec10fe5203fda4df339`, is the single canonical JS execution baseline. The existing `wild_buzzard_js` interpreter is transitional test/host-contract material and may not become a second live page heap. Brimstone remains a nested standalone workspace until its unsafe ownership/rooting boundary is hardened. Wasmtime v47.0.3 is the selected candidate Wasm core behind a browser-owned adapter; WASI is not exposed to web pages.
- Tests run and results: the final pre-integration `git ls-remote --symref` freshness check caught and advanced to a new upstream `master`/HEAD before commit; detached checkout and `git archive` produced 1,554 tracked files; every imported upstream file compared byte-for-byte and only the provenance note was extra; no nested `.git`, submodule, LFS omission, generated index, or build output was imported. `cargo metadata --manifest-path js/brimstone/Cargo.toml --locked --no-deps` passed from the live tree. An independent live-tree `cargo test --manifest-path js/brimstone/Cargo.toml --workspace --locked --target x86_64-unknown-linux-gnu` passed 11 Rust/snapshot tests with 0 failures. The refreshed release integration harness with `--reindex --ignore-unimplemented` passed 184 Brimstone integration tests and 44,867 selected Test262 cases with 0 failures and 8,419 skips in 4.37 seconds.
- Parity evidence: the 44,867/0 normal harness result proves that the admitted upstream snapshot reproduces upstream's selected green test configuration. It is not a full Test262 percentage: `--ignore-unimplemented` skips configured known failures, SharedArrayBuffer/Atomics and other unimplemented features; the normal configuration also excludes Annex B, ECMA-402 `intl402`, other configured non-standard/proposal cases, and slow tests. A metadata-aware full report which counts every standards-required skip/failure remains mandatory.
- Known behavioral differences: no native JavaScript JIT, safe browser embedding, DOM host objects, asynchronous browser module loader, hard interrupt, SharedArrayBuffer/Atomics/agents, JavaScript `WebAssembly` API, complete Intl/Annex B evidence, partitioned generational/incremental browser GC, multi-process memory policy, debugger, or browser conformance integration. Upstream's module loader and task loop are not browser host contracts.
- Unsafe or FFI introduced: the snapshot contains extensive upstream unsafe pointer/GC/VM code. Its public `Context` is copyable and exposes safe raw construction/manual destruction, while lifetime-free handles and mutable dereferencing permit concrete double-free/use-after-free/aliasing risks. Default Linux features build third-party jemalloc through `tikv-jemallocator`; `tests/fuzz/coverage.c` is test-only native coverage support and not production code. None is yet admitted across an untrusted browser boundary.
- Licenses and provenance: exact independent upstream MIT source; `LICENSE`, all tests, ICU data, tools, manifests, lockfile, and assets are preserved. Provenance is recorded in `docs/upstream-components.toml` and the adjacent upstream note. The snapshot currently has a lock-pinned Git dependency on the author's `indexmap-allocator-api` fork; this must be vendored or otherwise admitted before reproducible offline product builds.
- Provider or network implications: no telemetry or runtime provider endpoint was found. Cargo may access crates.io and the pinned Git dependency during dependency resolution; Test262 installation is a development-only network action. Brimstone's current synchronous filesystem module loader is prohibited as the browser loader.
- Blocked on: nothing for continued engine work. Untrusted-page activation is deliberately blocked until owned-context/handle hardening, forced-GC/rooting stress, browser host boundaries, limits/interrupts, JIT safepoints, and full conformance gates pass.
- Recommended next action: first replace the unsafe public context/handle ownership surface with a non-copying, exactly-once, thread-affine Wild Buzzard facade and executable forced-GC tests. Then implement the stable helper ABI/opcode metadata, bounded W^X code cache, and the first Cranelift baseline-JIT arithmetic/control-flow/property-cache slice described in `AGENTS.md`. Import the exact Wasmtime stable subset separately after its feature/dependency audit.
