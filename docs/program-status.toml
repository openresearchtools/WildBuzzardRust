schema = 1
project = "Wild Buzzard"
updated_at = "2026-08-09"
overall_status = "implementation-foundation"
runnable_browser = false
firefox_engine_parity = false
firefox_ui_parity = false
normal_websites_verified = false
youtube_verified = false
supported_rust_target = "x86_64-unknown-linux-gnu"
release_artifact = "AppImage"

[baseline]
firefox_branch = "esr153"
firefox_revision = "c19b7e89270787889495688244ec6ee8e79288a1"
reference_checkout = "firefox"
reference_is_build_input = false

[completion]
summary = "The program completes only when the Rust-native browser and UI run normal websites, including JavaScript-heavy sites such as YouTube, and the recorded Firefox ESR engine/UI parity gates pass."
required_evidence = [
  "Rust-native Linux x86_64 browser executable and browser chrome",
  "No first-party C or C++ runtime implementation and no dependency on the firefox reference checkout",
  "Test262 and WebAssembly specification conformance reports",
  "Web Platform Test, CSS, reftest, accessibility, WebDriver, network, storage, media, and security reports",
  "Normal-site compatibility suite with JavaScript enabled",
  "YouTube navigation, script execution, layout, input, audio/video playback, and recovery smoke tests",
  "Firefox ESR engine behavior and Rust UI capability parity matrix with every gap closed or explicitly accepted",
  "Relocatable Linux x86_64 AppImage plus privacy/prohibited-endpoint audit",
]

[orchestration]
max_concurrent_agents = 4
main_agent_role = "integration, review, root manifests, contracts, tests, parity evidence, commits"
external_build_root = "../wildbuzzardbuilds"
commit_policy = "Integrate and push coherent, reviewed waves; agents do not commit independently."
javascript_lane_policy = "Keep Agent 2 continuously staffed. JS, Wasm, GC, rooting, host bindings, and optimization remain one critical architectural program."
javascript_engine = "Brimstone exact upstream baseline at b544eff181ef6a72639f26a89b6aca1f8d6e6b50; prohibited for untrusted browser content until hardening gates pass"
javascript_jit_target = "Cranelift baseline and optimizing tiers for x86_64-unknown-linux-gnu"
webassembly_candidate = "Wasmtime v47.0.3 at 5554cc1a651da536af2cc46c7324bdc085b162e3 with minimal Cranelift core features behind a Wild Buzzard browser-owned JS/Wasm boundary; no WASI exposure"

[current_wave]
id = "W1"
name = "engine nuclei and contracts"
state = "complete"
integration_goal = "A root-integrated Rust workspace containing tested foundation, JS embedding, DOM/HTML, and layout contracts without claiming a runnable browser."

[[current_wave.task]]
id = "W1-A1"
owner = "agent-1-foundation-platform"
model = "gpt-5.6-sol"
reasoning_effort = "high"
state = "complete"
write_paths = ["xpcom", "ipc", "widget", "memory", "mozglue", "intl"]
acceptance = [
  "Generational handles reject stale identities",
  "Versioned size-bounded IPC validates malformed and oversized input",
  "Cancellation and lifecycle transitions are deterministic",
  "Platform-neutral geometry/input/surface types have contract tests",
]
evidence = [
  "Five crates integrated into the root workspace and lockfile",
  "37 foundation tests passed independently and in the root workspace",
  "Root check, scoped strict Clippy, release build, and rustdoc gates passed on x86_64-unknown-linux-gnu",
  "docs/handoffs/W1-A1-foundation.md",
]

[[current_wave.task]]
id = "W1-A2"
owner = "agent-2-js-wasm"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = ["js"]
acceptance = [
  "Native Rust runtime with no C/C++ engine wrapper",
  "Stable rooted host API and explicit Engine/Realm/Context boundaries",
  "Meaningful initial ECMAScript execution with structured errors and semantic tests",
  "Heap/handle architecture can grow into tracing GC and shared JS/Wasm rooting",
]
evidence = [
  "Rust-native interpreter and embedding API integrated into the root workspace",
  "36 unit, embedding, and semantic tests passed in owner and orchestrator gates",
  "Root-integrated package check, strict Clippy, release build, and rustdoc gates passed",
  "docs/handoffs/W1-A2-javascript.md",
]

[[current_wave.task]]
id = "W1-A2B"
owner = "agent-2-js-wasm"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = ["js"]
acceptance = [
  "Safe stop-the-world tracing mark/sweep collector preserves the embedding API",
  "All roots, globals, properties, functions, and lexical-environment edges are traced",
  "Unreachable cycles are reclaimed and reused slots cannot alias stale handles",
  "Collection is restricted to proven safe points with graph and failure-recovery tests",
]
evidence = [
  "Non-moving mark/sweep collector and typed reusable generational arenas integrated in js",
  "54 total JavaScript tests passed, including 12 focused GC integration tests",
  "Root-integrated formatting, check, strict Clippy, locked tests, release, and rustdoc gates passed",
  "docs/handoffs/W1-A2B-javascript-gc.md",
]

[[current_wave.task]]
id = "W1-A3"
owner = "agent-3-dom-style-layout"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = ["dom", "parser", "layout"]
acceptance = [
  "Stable safe DOM node identities and mutation invariants",
  "Incremental HTML tokenization and tree construction with source-positioned errors",
  "Immutable layout snapshots and initial block/inline box output",
  "Explicit future Stylo and rooted JS binding boundaries",
]
evidence = [
  "DOM, HTML parser, and static layout crates integrated into the root workspace",
  "39 tests passed in owner and independent orchestrator gates",
  "Root-integrated formatting, check, strict Clippy, locked tests, release, and rustdoc gates passed",
  "docs/handoffs/W1-A3-dom-html-layout.md",
]

[[milestone]]
id = "M0"
name = "source and orchestration foundation"
state = "complete"
evidence = [
  "AGENTS.md",
  "docs/component-map.md",
  "docs/upstream-components.toml",
  "docs/import-validation.md",
]

[[milestone]]
id = "M1"
name = "headless static page"
state = "in-progress"
required = [
  "URL parsing",
  "loopback HTTP fetch",
  "HTML parsing",
  "DOM",
  "Stylo-backed computed style",
  "layout",
  "display list",
  "WebRender frame and deterministic screenshot",
]

[[milestone]]
id = "M2"
name = "scripted page and DOM bindings"
state = "pending"
required = [
  "JavaScript runtime",
  "tracing GC and rooted DOM bindings",
  "event loop and jobs",
  "script loading and execution",
  "DOM mutation followed by style/layout/frame update",
  "focused Test262 and WPT evidence",
]

[[milestone]]
id = "M3"
name = "Rust-native desktop browser shell"
state = "pending"
required = [
  "Rust Linux window and compositor surface with required Wayland/X11 paths",
  "tabs and address bar",
  "navigation, input, prompts, permissions, downloads, and sessions",
  "accessibility tree and keyboard operation",
  "WebDriver smoke tests",
]

[[milestone]]
id = "M4"
name = "modern site capability"
state = "pending"
required = [
  "TLS, HTTP/2, HTTP/3, caching, cookies, CSP, CORS, and isolation",
  "modules, promises, workers, WebAssembly, Canvas, WebGL, and WebGPU",
  "IndexedDB and partitioned origin storage",
  "Media Source, audio/video decoding and playback",
  "service workers, extensions, developer tools, and recovery",
]

[[milestone]]
id = "M5"
name = "Firefox ESR engine and UI parity"
state = "pending"
required = [
  "All conformance and product parity matrices supported by reproducible evidence",
  "Normal-site suite passes",
  "YouTube end-to-end smoke and interaction suite passes",
  "Linux x86_64 security, privacy, performance, accessibility, and AppImage release gates pass",
]

[next_wave]
id = "W2"
state = "in-progress"
tasks = [
  "Agent 5 complete for W2 nucleus: bounded loopback HTTP transport, URL/request contracts, cancellation, and fail-closed framing",
  "Agent 4 complete for W2 graphics boundaries: validated layout-to-WebRender scene plus real Linux EGL/WebRender submission and deterministic RGBA8 pixels",
  "Agent 3 complete for W2 Stylo admission: independently locked generated CSS engine with Rust platform shims and prohibited Gecko paths; concrete DOM/computed-style adaptation remains next",
  "Agent 2 complete for W2 object-model slice: descriptors, prototypes, construction, sparse Arrays, and all new GC edges",
  "Agent 2 complete for W2 string slice: exact UTF-16 code units, owned property keys, embedding boundaries, and primitive indexing",
  "Agent 6: headless engine facade and Rust-native Linux window/surface shell",
  "Agent 2 complete for W2 Symbol slice: fresh Symbols, unified PropertyKey storage, tracing, and ordered own-key reflection",
  "Agent 2: import and harden pinned Brimstone, add its Linux x86-64 JIT tiers, integrate selected Wasmtime core behind browser JS/Wasm contracts, then generated DOM bindings",
]

[[next_wave.work_item]]
id = "W2-A5"
owner = "agent-5-network-security-storage"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = ["netwerk/rust/wild_buzzard_net"]
acceptance = [
  "Numeric-loopback-only HTTP/1.1 transport cannot invoke DNS or reach a non-loopback address",
  "Request and response metadata, bodies, chunks, trailers, and informational responses are bounded",
  "Cancellation, absolute deadlines, and inactivity timeouts use the shared runtime contract",
  "Malformed framing and terminal body failures are latched so retries cannot expose later peer bytes",
]
evidence = [
  "wild_buzzard_net integrated into the root workspace with the imported local WHATWG url crate",
  "37 deterministic loopback integration tests passed, including fail-closed retry regressions",
  "Root-integrated formatting, check, strict Clippy, locked tests, release, and rustdoc package gates passed",
  "docs/handoffs/W2-A5-network.md",
]

[[next_wave.work_item]]
id = "W2-A4"
owner = "agent-4-graphics-media"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = ["gfx/wild_buzzard_renderer"]
acceptance = [
  "Immutable layout output is validated before becoming renderer-owned scene data",
  "Supported backgrounds, borders, viewport clip, and clip chain serialize through the real imported WebRender API",
  "Unshaped text remains an explicit typed pending resource rather than receiving fabricated glyphs",
  "Graph, geometry, text, scene, traversal, and WebRender serialization resources are bounded",
]
evidence = [
  "wild_buzzard_renderer integrated into the root workspace with imported webrender_api and peek-poke Rust crates",
  "17 renderer integration tests passed, including built-list round trip, allocation boundary, and out-of-viewport clip regressions",
  "Root-integrated formatting, check, strict Clippy, locked tests, release, and rustdoc package gates passed",
  "docs/handoffs/W2-A4-renderer.md",
]

[[next_wave.work_item]]
id = "W2-A2C"
owner = "agent-2-js-wasm"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = ["js"]
acceptance = [
  "Ordinary objects store complete data/accessor descriptors while definition inputs preserve absent fields",
  "Prototype lookup and mutation are receiver-correct, bounded, cycle-safe, and traced by the collector",
  "Ordinary construction follows argument-order, prototype-derived allocation, and object-return override invariants",
  "Sparse Arrays distinguish holes from undefined, enforce canonical indices and length rules, and trace every element descriptor",
]
evidence = [
  "Rust-native Object/Array intrinsic subset and parser/runtime support integrated without dependencies or unsafe code",
  "70 JavaScript tests passed, including 15 object-model integration tests and GC regressions for every new edge",
  "Orchestrator direct-file formatting, check, strict all-target/all-feature Clippy, locked tests, release, and rustdoc gates passed on x86_64-unknown-linux-gnu",
  "docs/handoffs/W2-A2C-javascript-objects.md",
]

[[next_wave.work_item]]
id = "W2-A4B"
owner = "agent-4-graphics-media"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = ["gfx/wild_buzzard_headless"]
acceptance = [
  "A real imported WebRender renderer submits the validated scene to a Linux x86_64 EGL pbuffer and returns owned bounded RGBA8 pixels",
  "The exact RGB8/A8 zero-sample context, submitted revision/epoch, viewport, display-list bytes, scene resources, output allocation, and asynchronous waits are validated",
  "Frame publication cannot race notification ingestion, and multiple live same-thread renderer contexts are restored before every GL operation",
  "Construction, shutdown, deinitialization, and EGL release fail closed; a driver that refuses to unbind cannot trigger destruction of current native objects",
]
evidence = [
  "wild_buzzard_headless integrated into the root workspace with imported WebRender, glutin EGL, and gleam GL boundaries",
  "9 unit tests and 3 real EGL/WebRender integration tests passed, including deterministic known pixels and alternating two live renderers",
  "Independent lifecycle, notification, unsafe, dependency, and claim audit passed after closing a queue-order race and context-ownership failure paths",
  "Root-integrated formatting, check, strict all-target/all-feature Clippy, locked tests, release, and rustdoc gates passed on x86_64-unknown-linux-gnu",
  "docs/handoffs/W2-A4B-headless-renderer.md",
]

[[next_wave.work_item]]
id = "W2-A4C"
owner = "agent-4-graphics-media"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = [
  "gfx/wild_buzzard_text",
  "gfx/wild_buzzard_text_webrender",
  "gfx/wild_buzzard_headless",
]
acceptance = [
  "Rust Parley, Fontique, HarfRust, Fontations, and ICU4X components select and shape bounded text into an immutable layout-owned contract",
  "The exact shaped font blobs, face indices, glyph identifiers, clusters, run metrics, baselines, offsets, and advances reach WebRender without selection or reshaping in the renderer adapter",
  "Font templates and instances are namespace-scoped, bounded, transactionally admitted, reused only for exact descriptors, and explicitly released instance-before-template",
  "A real Linux EGL/WebRender pixel test proves glyph placement, clipping, color/alpha semantics, pipeline replacement, resource reuse, and teardown",
]
evidence = [
  "27 focused unit/integration tests passed, including a real HarfRust-to-WebRender glyph frame on the NVIDIA EGL/OpenGL path",
  "Independent source, lifecycle, UTF-8 range, allocation, cache-accounting, resource-limit, and pixel-semantics audit passed after transactional registration and pipeline cleanup fixes",
  "Exact crate revisions, licenses, Fira Code hashes, and remaining Fontconfig/FreeType/mapping boundaries are recorded in docs/upstream-components.toml",
  "Focused formatting, strict all-target/all-feature Clippy, locked tests, release, and warning-denied rustdoc gates passed from external build trees",
  "docs/handoffs/W2-A4C-rust-text.md",
]

[[next_wave.work_item]]
id = "W2-A2D"
owner = "agent-2-js-wasm"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = ["js"]
acceptance = [
  "JavaScript strings preserve arbitrary UTF-16 code units, including lone surrogates and embedded NUL, through public embedding snapshots",
  "String literals, equality, hashing, ordering, concatenation, length, indexing, numeric conversion, and primitive deletion use code-unit semantics",
  "Object property keys own exact immutable string content and remain valid after collection of the originating string value",
  "Dynamic public value, property-key, function-name, and caught-error conversions into JsString enforce the ESR153 maximum fallibly; unsupported legacy escapes and strict-mode behavior are explicit rather than silently fabricated",
]
evidence = [
  "Immutable Arc-backed JsString and content-owned property keys integrated without unsafe code or dependencies",
  "87 JavaScript tests passed after independent review caught and the orchestrator fixed dynamic length panics plus complete U+2028/U+2029 continuation, source-line, ASI, and single-line-comment handling",
  "Direct-file Rust 2024 formatting, package check, strict all-target/all-feature Clippy, locked tests, release, rustdoc, root check, and root tests passed on x86_64-unknown-linux-gnu",
  "docs/handoffs/W2-A2D-javascript-strings.md",
]

[[next_wave.work_item]]
id = "W2-A3S"
owner = "agent-3-dom-style-layout"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = ["servo"]
acceptance = [
  "The imported Stylo selector, generated-property, cascade, and computed-value core builds in an independently locked workspace without Gecko, XPCOM, C++, bindgen, or the Firefox checkout",
  "The only product target is checked as exact x86_64-unknown-linux-gnu and every Gecko feature is an asserted negative gate",
  "Wild Buzzard atoms, DOM/document state bits, static release preferences, thread policy, and profiler hooks are narrow Rust contracts with focused tests",
  "Unsupported stylesheet URL shared-memory transfer fails with a structured error before consuming output space",
  "Known DOM, computed-style, property-universe, device, font, unsafe, and imported-lint gaps remain explicit and cannot be mistaken for page or CSS parity",
]
evidence = [
  "The real Mako generator produced and compiled 107,660 lines of property code covering 256 longhands and 69 shorthands into the default style crate",
  "44 workspace tests passed with 3 ignored imported documentation examples; 18 passing tests directly cover the new CSS, cascade, selector, target, shim, and boundary contracts",
  "All-target check, release build, focused strict Clippy and rustdoc gates, full workspace rustdoc with four recorded imported-debt allowances, and the expected Gecko all-features rejection passed on x86_64-unknown-linux-gnu",
  "Independent audit passed after correcting selector-policy tests, shadow-parts expectations, target enforcement, preference provenance, URL fail-closed behavior, and overbroad generated-output and lint claims",
  "docs/handoffs/W2-A3S-stylo.md",
]

[[next_wave.work_item]]
id = "W2-A3T"
owner = "agent-3-dom-style-layout"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = [
  "servo/components/wild_buzzard_stylo_adapter",
  "servo/components/style/sharing/mod.rs",
  "layout",
]
acceptance = [
  "A bounded immutable DocumentSnapshot adapter implements Stylo's generic DOM traits without Gecko, XPCOM, bindgen, raw DOM pointers, or a live Servo DOM dependency",
  "Real author and temporary-UA stylesheets, selector matching, cascade, inheritance, element restyle completion, and root rem updates execute through imported Stylo APIs",
  "Exact document/revision selector state and computed-style publications fail closed on stale, conflicting, missing, duplicate, or wrong-document data",
  "Supported physical edges, colors, text values, width/height/min/max sizes, box sizing, and writing mode reach root static layout without fabricated intrinsic or vertical geometry",
  "HTML and SVG link matching remains on the unvisited path, incompatible preinitialized thread roles return structured errors, and all resource work is bounded",
]
evidence = [
  "20 concrete-adapter tests, 15 root-layout tests, 38 focused Stylo/selector tests, and 64 complete nested-workspace tests passed",
  "Root and adapter formatting, locked checks/tests, strict Clippy, release, and warning-denied rustdoc gates passed from external build trees; complete imported-workspace rustdoc passed with the four recorded upstream-debt allowances",
  "The prohibited Gecko all-features gate failed at the intended generator guard and active metadata contains no firefox checkout, bindgen, clang-sys, cc, XPCOM, or Gecko build dependency",
  "Independent review confirmed finish_restyle root-device updates, typed sizing/writing-mode projection, safe two-word adapter identity, and explicit remaining font/device/UA/live-DOM/layout gaps",
  "docs/handoffs/W2-A3T-stylo-layout.md",
]

[[next_wave.work_item]]
id = "W2-A2E"
owner = "agent-2-js-wasm"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = ["js"]
acceptance = [
  "Fresh generation-checked Symbol primitives preserve absent versus exact UTF-16 descriptions without exposing identity handles",
  "String and Symbol PropertyKeys share descriptor, lookup, definition, deletion, rooting, and tracing paths",
  "Ordinary objects and Arrays enumerate indices, strings, implicit Array length, and Symbols in the required stable category order",
  "Object.getOwnPropertyNames, Object.getOwnPropertySymbols, and Reflect.ownKeys share one bounded validated own-key source",
]
evidence = [
  "Rust-native Symbol arena and ordered unified PropertyKey storage integrated without dependencies or unsafe code",
  "100 JavaScript tests passed, including focused identity, exact-description, descriptor, ordering, Array-boundary, embedding, cross-realm-rejection, and GC regressions",
  "Independent audit found no blocking correctness or safety issue and corrected the documented conforming behavior of the ordinary Symbol prototype object",
  "Exact-file formatting, package all-target/all-feature check, strict Clippy, locked tests, release build, and warning-denied rustdoc passed on x86_64-unknown-linux-gnu using external targets",
  "docs/handoffs/W2-A2E-javascript-symbols.md",
]

[[next_wave.work_item]]
id = "W2-A2G"
owner = "agent-2-js-wasm"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "in-progress"
write_paths = ["js/brimstone", "future Wild Buzzard Brimstone host/JIT adapter paths under js"]
acceptance = [
  "Exact latest-selected Brimstone source snapshot and MIT license are present with reproducible provenance and no nested repository metadata",
  "The snapshot builds and its honest upstream tests run independently outside the repository before any adaptation",
  "Unsafe Context/handle ownership and moving-GC rooting blockers are isolated and converted into executable hardening gates",
  "A bounded W^X Cranelift baseline-JIT prototype compiles hot arithmetic, control flow, calls, and cached property access while forced collection at every safepoint remains correct",
  "A browser-owned Wasmtime integration design proves JS/Wasm values, cross-heap roots, memories, tables, exceptions, jobs, interrupts, and error mapping without WASI exposure",
]
evidence = [
  "Exact Brimstone b544eff source import and selected green upstream harness completed; docs/handoffs/W2-A2G-brimstone-import.md",
  "Brimstone owned-context/root-scope safety gate, heap teardown/resize fixes, focused clean ASan/LSan, and forced-GC evidence completed; docs/handoffs/W2-A2H-brimstone-safety.md",
  "Wasmtime v47.0.3 minimal-core audit passed product cargo check and recorded the upstream minimal-feature unit-test compile gap; docs/handoffs/W2-A2W-wasmtime-admission.md",
  "Pending Brimstone internal lifetime completion and limits, JIT prototype, exact Wasmtime source import, Wasm specification suite, full Test262 accounting, and browser integration tests",
]
