schema = 1
project = "Wild Buzzard"
updated_at = "2026-08-23"
overall_status = "implementation-foundation"
runnable_browser = false
firefox_engine_parity = false
firefox_ui_parity = false
normal_websites_verified = false
youtube_verified = false
supported_rust_target = "x86_64-unknown-linux-gnu"
release_artifact = "AppImage"

[checkpoint]
state = "active"
after_wave = "W8"
next_wave_started = true
resume_note = "W8 is the current reviewed integration checkpoint. W9 immediately connects authenticated top-level navigation to the real HTML/DOM/Stylo/layout/render path, closes generic CSS blockers found by public desktop probes, and advances rooted Brimstone-to-DOM task integration before visible-shell and Firefox ESR comparison work; there is no pause, and normal websites, YouTube, Firefox parity, and AppImage completion remain open."

[baseline]
firefox_branch = "esr153"
firefox_revision = "c19b7e89270787889495688244ec6ee8e79288a1"
reference_checkout = "firefox"
reference_is_build_input = false

[completion]
summary = "The program completes only when the Rust-native browser and UI run normal websites, including JavaScript-heavy sites such as YouTube, and the recorded Firefox ESR engine/UI parity gates pass."
required_evidence = [
  "Rust-native Linux x86_64 browser executable and browser chrome",
  "No first-party C or C++ runtime implementation and no dependency on the firefox reference checkout",
  "Test262 and WebAssembly specification conformance reports",
  "Web Platform Test, CSS, reftest, accessibility, WebDriver, network, storage, media, and security reports",
  "Normal-site compatibility suite with JavaScript enabled",
  "YouTube navigation, script execution, layout, input, audio/video playback, and recovery smoke tests",
  "Firefox ESR engine behavior and Rust UI capability parity matrix with every gap closed or explicitly accepted",
  "Relocatable Linux x86_64 AppImage plus privacy/prohibited-endpoint audit",
]

[orchestration]
max_concurrent_agents = 4
main_agent_role = "integration, review, root manifests, contracts, tests, parity evidence, commits"
external_build_root = "../wildbuzzardbuilds"
commit_policy = "Integrate and push coherent, reviewed waves; agents do not commit independently."
javascript_lane_policy = "Keep Agent 2 continuously staffed. JS, Wasm, GC, rooting, host bindings, and optimization remain one critical architectural program."
javascript_engine = "Brimstone exact upstream baseline at bfb720f0afb8b2b28b27c22ee7091deb7d16b082; prohibited for untrusted browser content until hardening gates pass"
javascript_jit_target = "Cranelift baseline and optimizing tiers for x86_64-unknown-linux-gnu"
javascript_jit_status = "W7-A2Q extends the disabled canonical Brimstone path through rooted typed constants and ordinary closure creation with moving-root and continuation invariants; product dispatch remains compile-time false. W8-A2R completes an independently reviewed bounded owner-thread classic-script admission seam but remains NO-GO for untrusted pages."
webassembly_core = "Selected imported Wasmtime v47.0.3 source at 5554cc1a651da536af2cc46c7324bdc085b162e3 plus the product-disconnected first-party js/wasm adapter with minimal Cranelift core features; no WASI exposure"
static_integration = "W2-A6C connects W2-A6, W2-A4D, and W2-A6N: the bounded synchronous engine retains every canonical finalized shape, submits one exact-scene-bound composed zero-pending frame, and publishes it through a generation-aware lease; there is still no window or UI behavior"
dynamic_integration = "W4-A6E exposes exact-navigation mutation, rerender, and close through the bounded multi-context worker; DOM-issued commit proofs, retained-plus-pending node accounting, permanent context-ID retirement, navigation-only replacement cleanup, and engine-owned never-reused document-operation cancellation identities fail closed; no JS/task loop, incremental invalidation, presentation, UI, or untrusted-page activation"
linux_window_prerequisite = "W7-A4S/A6H compose a typed Firefox-shaped primary chrome scene and functional Rust interaction state on the exact Wayland/X11 EGL surface. Normal-size live runs pass with checked resize ordering and receipt-bound input; EGL swap submission is proved, but desktop-compositor display is not."
browser_session_controller = "W7-A6H connects typed tabs, navigation controls, address editing, panels, focus, pointer capture, popup scrolling, session actions, and the Linux presenter in the Rust executable. W9-A6J selects the separate general HTTP/authenticated-HTTPS presentation capability consistently for ordinary startup, address, history, and reload while retaining the narrow loopback smoke; untrusted page script, redirect/final-security publication, persistence, AT-SPI, isolation, AppImage, normal-site proof, and Firefox UI parity remain open."

[wave1]
id = "W1"
name = "engine nuclei and contracts"
state = "complete"
integration_goal = "A root-integrated Rust workspace containing tested foundation, JS embedding, DOM/HTML, and layout contracts without claiming a runnable browser."

[[wave1.task]]
id = "W1-A1"
owner = "agent-1-foundation-platform"
model = "gpt-5.6-sol"
reasoning_effort = "high"
state = "complete"
write_paths = ["xpcom", "ipc", "widget", "memory", "mozglue", "intl"]
acceptance = [
  "Generational handles reject stale identities",
  "Versioned size-bounded IPC validates malformed and oversized input",
  "Cancellation and lifecycle transitions are deterministic",
  "Platform-neutral geometry/input/surface types have contract tests",
]
evidence = [
  "Five crates integrated into the root workspace and lockfile",
  "37 foundation tests passed independently and in the root workspace",
  "Root check, scoped strict Clippy, release build, and rustdoc gates passed on x86_64-unknown-linux-gnu",
  "docs/handoffs/W1-A1-foundation.md",
]

[[wave1.task]]
id = "W1-A2"
owner = "agent-2-js-wasm"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = ["js"]
acceptance = [
  "Native Rust runtime with no C/C++ engine wrapper",
  "Stable rooted host API and explicit Engine/Realm/Context boundaries",
  "Meaningful initial ECMAScript execution with structured errors and semantic tests",
  "Heap/handle architecture can grow into tracing GC and shared JS/Wasm rooting",
]
evidence = [
  "Rust-native interpreter and embedding API integrated into the root workspace",
  "36 unit, embedding, and semantic tests passed in owner and orchestrator gates",
  "Root-integrated package check, strict Clippy, release build, and rustdoc gates passed",
  "docs/handoffs/W1-A2-javascript.md",
]

[[wave1.task]]
id = "W1-A2B"
owner = "agent-2-js-wasm"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = ["js"]
acceptance = [
  "Safe stop-the-world tracing mark/sweep collector preserves the embedding API",
  "All roots, globals, properties, functions, and lexical-environment edges are traced",
  "Unreachable cycles are reclaimed and reused slots cannot alias stale handles",
  "Collection is restricted to proven safe points with graph and failure-recovery tests",
]
evidence = [
  "Non-moving mark/sweep collector and typed reusable generational arenas integrated in js",
  "54 total JavaScript tests passed, including 12 focused GC integration tests",
  "Root-integrated formatting, check, strict Clippy, locked tests, release, and rustdoc gates passed",
  "docs/handoffs/W1-A2B-javascript-gc.md",
]

[[wave1.task]]
id = "W1-A3"
owner = "agent-3-dom-style-layout"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = ["dom", "parser", "layout"]
acceptance = [
  "Stable safe DOM node identities and mutation invariants",
  "Incremental HTML tokenization and tree construction with source-positioned errors",
  "Immutable layout snapshots and initial block/inline box output",
  "Explicit future Stylo and rooted JS binding boundaries",
]
evidence = [
  "DOM, HTML parser, and static layout crates integrated into the root workspace",
  "39 tests passed in owner and independent orchestrator gates",
  "Root-integrated formatting, check, strict Clippy, locked tests, release, and rustdoc gates passed",
  "docs/handoffs/W1-A3-dom-html-layout.md",
]

[[milestone]]
id = "M0"
name = "source and orchestration foundation"
state = "complete"
evidence = [
  "AGENTS.md",
  "docs/component-map.md",
  "docs/upstream-components.toml",
  "docs/import-validation.md",
]

[[milestone]]
id = "M1"
name = "headless static page"
state = "complete"
required = [
  "URL parsing",
  "loopback HTTP fetch",
  "HTML parsing",
  "DOM",
  "Stylo-backed computed style",
  "layout",
  "display list",
  "One WebRender frame containing page primitives and all positioned shaped text, with a deterministic screenshot",
  "Typed navigation generation, cancellation, and atomic stale-frame suppression",
]
evidence = [
  "W2-A6 independently locked synchronous loopback URL-to-real-WebRender integration proof passes",
  "Typed DocumentVersion flows through DOM, layout, scene, one composed frame, and evidence without cross-document revision comparison",
  "W2-A6C retains every canonical finalized shaped run, including whitespace, projects the exact first baseline, and submits page primitives and all positioned glyphs in one W2-A4D transaction with zero pending text",
  "W2-A6N publishes that composed result through bounded typed commands, sequenced events, one-shot frame leases, monotonic navigation generations, cancellation, and atomic stale-publication suppression; it adds no window or UI",
  "Independent W2-A6C review returned GO; after both low evidence gaps were closed, 24 locked tests passed, including deterministic repeated pixels, an identified leased glyph pixel, pending-frame rejection, and a real numeric-loopback navigation worker/lease fixture",
  "docs/handoffs/W2-A6-static-pipeline.md",
  "docs/handoffs/W2-A6N-navigation-facade.md",
  "docs/handoffs/W2-A4D-composed-text-frame.md",
  "docs/handoffs/W2-A6C-composed-pipeline.md",
]

[[milestone]]
id = "M2"
name = "scripted page and DOM bindings"
state = "pending"
required = [
  "JavaScript runtime",
  "tracing GC and rooted DOM bindings",
  "event loop and jobs",
  "script loading and execution",
  "DOM mutation followed by style/layout/frame update",
  "focused Test262 and WPT evidence",
]

[[milestone]]
id = "M3"
name = "Rust-native desktop browser shell"
state = "pending"
required = [
  "Rust Linux window and compositor surface with required Wayland/X11 paths",
  "tabs and address bar",
  "navigation, input, prompts, permissions, downloads, and sessions",
  "accessibility tree and keyboard operation",
  "WebDriver smoke tests",
]

[[milestone]]
id = "M4"
name = "modern site capability"
state = "pending"
required = [
  "TLS, HTTP/2, HTTP/3, caching, cookies, CSP, CORS, and isolation",
  "modules, promises, workers, WebAssembly, Canvas, WebGL, and WebGPU",
  "IndexedDB and partitioned origin storage",
  "Media Source, audio/video decoding and playback",
  "service workers, extensions, developer tools, and recovery",
]

[[milestone]]
id = "M5"
name = "Firefox ESR engine and UI parity"
state = "pending"
required = [
  "All conformance and product parity matrices supported by reproducible evidence",
  "Normal-site suite passes",
  "YouTube end-to-end smoke and interaction suite passes",
  "Linux x86_64 security, privacy, performance, accessibility, and AppImage release gates pass",
]

[wave2]
id = "W2"
state = "in-progress"
tasks = [
  "Agent 5 complete for W2 nucleus: bounded loopback HTTP transport, URL/request contracts, cancellation, and fail-closed framing",
  "Agent 4 complete for the accepted W2 graphics nuclei and exact-scene-bound W2-A4D composed-frame path; W2-A6C consumes it",
  "Agent 3 complete for W2 Stylo admission and immutable DOM/computed-style/layout adaptation; live invalidation and CSS/layout expansion remain",
  "Agent 2 complete for W2 object-model slice: descriptors, prototypes, construction, sparse Arrays, and all new GC edges",
  "Agent 2 complete for W2 string slice: exact UTF-16 code units, owned property keys, embedding boundaries, and primitive indexing",
  "Agent 6 complete for W2-A6 synchronous static integration, W2-A6N bounded generation-aware worker/event/lease publication, and W2-A6C composed-frame wiring; W3-A6W separately completes an event-shell prerequisite while compositor/presentation integration remains",
  "Agent 2 complete for W2 Symbol slice: fresh Symbols, unified PropertyKey storage, tracing, and ordered own-key reflection",
  "Agent 2: import and harden pinned Brimstone, add its Linux x86-64 JIT tiers, integrate selected Wasmtime core behind browser JS/Wasm contracts, then generated DOM bindings",
]

[wave3]
id = "W3"
name = "live-document and Linux-shell prerequisites"
state = "complete"
integration_goal = "Advance exact live-document recomposition, the canonical Brimstone JIT/VM path, and the Linux event shell without claiming browser script or presentation integration."
tasks = [
  "Agent 3 complete for W3-A3S: exact-version bounded atomic script-mutation transactions; no Brimstone wrapper, event loop, observer delivery, or incremental invalidation",
  "Agent 2 complete for W3-A2M contained proof: bounded local-CFG side-exit continuation in the actual Brimstone VM; native generation and product dispatch remain unchanged",
  "Agent 1 complete for W3-A6W prerequisite: reviewed Wayland/X11 window/event shell; Wild Buzzard renderer/compositor presentation-surface connection, UI, and AppImage closure remain",
  "Agent 6 complete for W3-A6D contained scope: one direct live document, exact bounded atomic mutation, full recomputation, exact rerender, and terminal renderer-health contract; no script/navigation/presentation activation",
]

[[wave2.work_item]]
id = "W2-A5"
owner = "agent-5-network-security-storage"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = ["netwerk/rust/wild_buzzard_net"]
acceptance = [
  "Numeric-loopback-only HTTP/1.1 transport cannot invoke DNS or reach a non-loopback address",
  "Request and response metadata, bodies, chunks, trailers, and informational responses are bounded",
  "Cancellation, absolute deadlines, and inactivity timeouts use the shared runtime contract",
  "Malformed framing and terminal body failures are latched so retries cannot expose later peer bytes",
]
evidence = [
  "wild_buzzard_net integrated into the root workspace with the imported local WHATWG url crate",
  "37 deterministic loopback integration tests passed, including fail-closed retry regressions",
  "Root-integrated formatting, check, strict Clippy, locked tests, release, and rustdoc package gates passed",
  "docs/handoffs/W2-A5-network.md",
]

[[wave2.work_item]]
id = "W2-A4"
owner = "agent-4-graphics-media"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = ["gfx/wild_buzzard_renderer"]
acceptance = [
  "Immutable layout output is validated before becoming renderer-owned scene data",
  "Supported backgrounds, borders, viewport clip, and clip chain serialize through the real imported WebRender API",
  "Unshaped text remains an explicit typed pending resource rather than receiving fabricated glyphs",
  "Graph, geometry, text, scene, traversal, and WebRender serialization resources are bounded",
]
evidence = [
  "wild_buzzard_renderer integrated into the root workspace with imported webrender_api and peek-poke Rust crates",
  "17 renderer integration tests passed, including built-list round trip, allocation boundary, and out-of-viewport clip regressions",
  "Root-integrated formatting, check, strict Clippy, locked tests, release, and rustdoc package gates passed",
  "docs/handoffs/W2-A4-renderer.md",
]

[[wave2.work_item]]
id = "W2-A2C"
owner = "agent-2-js-wasm"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = ["js"]
acceptance = [
  "Ordinary objects store complete data/accessor descriptors while definition inputs preserve absent fields",
  "Prototype lookup and mutation are receiver-correct, bounded, cycle-safe, and traced by the collector",
  "Ordinary construction follows argument-order, prototype-derived allocation, and object-return override invariants",
  "Sparse Arrays distinguish holes from undefined, enforce canonical indices and length rules, and trace every element descriptor",
]
evidence = [
  "Rust-native Object/Array intrinsic subset and parser/runtime support integrated without dependencies or unsafe code",
  "70 JavaScript tests passed, including 15 object-model integration tests and GC regressions for every new edge",
  "Orchestrator direct-file formatting, check, strict all-target/all-feature Clippy, locked tests, release, and rustdoc gates passed on x86_64-unknown-linux-gnu",
  "docs/handoffs/W2-A2C-javascript-objects.md",
]

[[wave2.work_item]]
id = "W2-A4B"
owner = "agent-4-graphics-media"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = ["gfx/wild_buzzard_headless"]
acceptance = [
  "A real imported WebRender renderer submits the validated scene to a Linux x86_64 EGL pbuffer and returns owned bounded RGBA8 pixels",
  "The exact RGB8/A8 zero-sample context, submitted revision/epoch, viewport, display-list bytes, scene resources, output allocation, and asynchronous waits are validated",
  "Frame publication cannot race notification ingestion, and multiple live same-thread renderer contexts are restored before every GL operation",
  "Construction, shutdown, deinitialization, and EGL release fail closed; a driver that refuses to unbind cannot trigger destruction of current native objects",
]
evidence = [
  "wild_buzzard_headless integrated into the root workspace with imported WebRender, glutin EGL, and gleam GL boundaries",
  "9 unit tests and 3 real EGL/WebRender integration tests passed, including deterministic known pixels and alternating two live renderers",
  "Independent lifecycle, notification, unsafe, dependency, and claim audit passed after closing a queue-order race and context-ownership failure paths",
  "Root-integrated formatting, check, strict all-target/all-feature Clippy, locked tests, release, and rustdoc gates passed on x86_64-unknown-linux-gnu",
  "docs/handoffs/W2-A4B-headless-renderer.md",
]

[[wave2.work_item]]
id = "W2-A4C"
owner = "agent-4-graphics-media"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = [
  "gfx/wild_buzzard_text",
  "gfx/wild_buzzard_text_webrender",
  "gfx/wild_buzzard_headless",
]
acceptance = [
  "Rust Parley, Fontique, HarfRust, Fontations, and ICU4X components select and shape bounded text into an immutable layout-owned contract",
  "The exact shaped font blobs, face indices, glyph identifiers, clusters, run metrics, baselines, offsets, and advances reach WebRender without selection or reshaping in the renderer adapter",
  "Font templates and instances are namespace-scoped, bounded, transactionally admitted, reused only for exact descriptors, and explicitly released instance-before-template",
  "A real Linux EGL/WebRender pixel test proves glyph placement, clipping, color/alpha semantics, pipeline replacement, resource reuse, and teardown",
]
evidence = [
  "27 focused unit/integration tests passed, including a real HarfRust-to-WebRender glyph frame on the NVIDIA EGL/OpenGL path",
  "Independent source, lifecycle, UTF-8 range, allocation, cache-accounting, resource-limit, and pixel-semantics audit passed after transactional registration and pipeline cleanup fixes",
  "Exact crate revisions, licenses, Fira Code hashes, and remaining Fontconfig/FreeType/mapping boundaries are recorded in docs/upstream-components.toml",
  "Focused formatting, strict all-target/all-feature Clippy, locked tests, release, and warning-denied rustdoc gates passed from external build trees",
  "docs/handoffs/W2-A4C-rust-text.md",
]

[[wave2.work_item]]
id = "W2-A4D"
owner = "agent-4-graphics-media"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = [
  "gfx/wild_buzzard_renderer",
  "gfx/wild_buzzard_text_webrender",
  "gfx/wild_buzzard_headless",
]
acceptance = [
  "Every supplied shaped layout record resolves exactly once to the corresponding pending scene slot with checked document, text, metrics, order, namespace, geometry, and aggregate limits",
  "An exact scene token binds prepared text to the one compiled scene it may complete and rejects a different same-version scene before renderer submission",
  "One bounded WebRender transaction installs font resources, page primitives, all positioned glyphs, root pipeline, epoch, notification, and frame generation with zero pending text",
  "Deterministic Linux EGL/WebRender tests prove multiple positioned runs contribute to one frame and preserve retry, poisoning, and teardown invariants",
]
evidence = [
  "The graphics path demonstrates one composed RGBA8 frame containing admitted page primitives and all supplied positioned shaped runs with zero pending text",
  "A private checked non-reusing identity flows from compilation through text resolution; separate differing-text and byte-identical scene probes reject cross-composition before mutation",
  "Independent post-fix review returned GO; 48 locked tests passed (23 renderer, 5 text adapter, 20 headless), including four real EGL composition tests, with strict Clippy and warning-denied rustdoc green",
  "W2-A6C now calls render_composed with the canonical finalized shaped inventory and publishes the zero-pending result through W2-A6N",
  "docs/handoffs/W2-A4D-composed-text-frame.md",
]

[[wave2.work_item]]
id = "W2-A2D"
owner = "agent-2-js-wasm"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = ["js"]
acceptance = [
  "JavaScript strings preserve arbitrary UTF-16 code units, including lone surrogates and embedded NUL, through public embedding snapshots",
  "String literals, equality, hashing, ordering, concatenation, length, indexing, numeric conversion, and primitive deletion use code-unit semantics",
  "Object property keys own exact immutable string content and remain valid after collection of the originating string value",
  "Dynamic public value, property-key, function-name, and caught-error conversions into JsString enforce the ESR153 maximum fallibly; unsupported legacy escapes and strict-mode behavior are explicit rather than silently fabricated",
]
evidence = [
  "Immutable Arc-backed JsString and content-owned property keys integrated without unsafe code or dependencies",
  "87 JavaScript tests passed after independent review caught and the orchestrator fixed dynamic length panics plus complete U+2028/U+2029 continuation, source-line, ASI, and single-line-comment handling",
  "Direct-file Rust 2024 formatting, package check, strict all-target/all-feature Clippy, locked tests, release, rustdoc, root check, and root tests passed on x86_64-unknown-linux-gnu",
  "docs/handoffs/W2-A2D-javascript-strings.md",
]

[[wave2.work_item]]
id = "W2-A3S"
owner = "agent-3-dom-style-layout"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = ["servo"]
acceptance = [
  "The imported Stylo selector, generated-property, cascade, and computed-value core builds in an independently locked workspace without Gecko, XPCOM, C++, bindgen, or the Firefox checkout",
  "The only product target is checked as exact x86_64-unknown-linux-gnu and every Gecko feature is an asserted negative gate",
  "Wild Buzzard atoms, DOM/document state bits, static release preferences, thread policy, and profiler hooks are narrow Rust contracts with focused tests",
  "Unsupported stylesheet URL shared-memory transfer fails with a structured error before consuming output space",
  "Known DOM, computed-style, property-universe, device, font, unsafe, and imported-lint gaps remain explicit and cannot be mistaken for page or CSS parity",
]
evidence = [
  "The real Mako generator produced and compiled 107,660 lines of property code covering 256 longhands and 69 shorthands into the default style crate",
  "44 workspace tests passed with 3 ignored imported documentation examples; 18 passing tests directly cover the new CSS, cascade, selector, target, shim, and boundary contracts",
  "All-target check, release build, focused strict Clippy and rustdoc gates, full workspace rustdoc with four recorded imported-debt allowances, and the expected Gecko all-features rejection passed on x86_64-unknown-linux-gnu",
  "Independent audit passed after correcting selector-policy tests, shadow-parts expectations, target enforcement, preference provenance, URL fail-closed behavior, and overbroad generated-output and lint claims",
  "docs/handoffs/W2-A3S-stylo.md",
]

[[wave2.work_item]]
id = "W2-A3T"
owner = "agent-3-dom-style-layout"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = [
  "servo/components/wild_buzzard_stylo_adapter",
  "servo/components/style/sharing/mod.rs",
  "layout",
]
acceptance = [
  "A bounded immutable DocumentSnapshot adapter implements Stylo's generic DOM traits without Gecko, XPCOM, bindgen, raw DOM pointers, or a live Servo DOM dependency",
  "Real author and temporary-UA stylesheets, selector matching, cascade, inheritance, element restyle completion, and root rem updates execute through imported Stylo APIs",
  "Exact document/revision selector state and computed-style publications fail closed on stale, conflicting, missing, duplicate, or wrong-document data",
  "Supported physical edges, colors, text values, width/height/min/max sizes, box sizing, and writing mode reach root static layout without fabricated intrinsic or vertical geometry",
  "HTML and SVG link matching remains on the unvisited path, incompatible preinitialized thread roles return structured errors, and all resource work is bounded",
]
evidence = [
  "20 concrete-adapter tests, 15 root-layout tests, 38 focused Stylo/selector tests, and 64 complete nested-workspace tests passed",
  "Root and adapter formatting, locked checks/tests, strict Clippy, release, and warning-denied rustdoc gates passed from external build trees; complete imported-workspace rustdoc passed with the four recorded upstream-debt allowances",
  "The prohibited Gecko all-features gate failed at the intended generator guard and active metadata contains no firefox checkout, bindgen, clang-sys, cc, XPCOM, or Gecko build dependency",
  "Independent review confirmed finish_restyle root-device updates, typed sizing/writing-mode projection, safe two-word adapter identity, and explicit remaining font/device/UA/live-DOM/layout gaps",
  "docs/handoffs/W2-A3T-stylo-layout.md",
]

[[wave2.work_item]]
id = "W2-A6"
owner = "agent-6-product-ui-tooling"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = [
  "browser/wild_buzzard_engine",
  "typed document-version propagation across dom, layout, and gfx owners",
]
acceptance = [
  "One bounded synchronous operation connects numeric-loopback HTTP, explicit UTF-8 HTML, immutable DOM, imported Stylo, layout using Rust-shaped metrics, scene compilation, and real Linux EGL/WebRender readback",
  "At the original W2-A6 gate, one typed DocumentVersion flowed unchanged through snapshot, computed style, layout, scene, page frame, shaped-glyph proof, and evidence without synthetic cross-document revision rebasing",
  "At the original W2-A6 gate, every pending text run was shaped and CompositionStatus honestly distinguished the separate proof; W2-A6C later retired that transitional API",
  "Cancellation, deadlines, malformed input, sequential document identity, exact pixels, resource ownership, and shutdown receive focused integration evidence",
]
evidence = [
  "60 focused lower-layer tests and all focused doc tests passed after the typed identity migration",
  "Two independently locked integration tests passed across separate-glyph, no-text, whitespace, 404, invalid UTF-8, cancellation, deadline, sequential-document, exact-pixel, and shutdown paths",
  "Focused strict Clippy, nested check, release build, and warning-denied no-dependency rustdoc passed from external task-owned build directories",
  "At W2-A6 acceptance, page primitives and glyphs were separate frames and no navigation-generation/event facade existed; W2-A6N added the facade and W2-A6C subsequently replaced the split with one composed frame",
  "W2-A6C completes M1's bounded headless result; normal network/script/window/UI behavior remains absent",
  "docs/handoffs/W2-A6-static-pipeline.md",
]

[[wave2.work_item]]
id = "W2-A6N"
owner = "agent-6-product-ui-tooling"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = ["browser/wild_buzzard_engine"]
acceptance = [
  "A dedicated worker constructs, executes, shuts down, and explicitly destroys the non-Send synchronous executor on its owner thread with stable exactly-once terminal status",
  "Opaque context IDs, monotonic navigation generations, bounded requests/queues/contexts/frame retention, typed commands/events/failures, and one-shot frame leases form a UI-neutral in-process facade",
  "Navigate admission is transactional, newer accepted generations cancel predecessors, and publication rechecks the current generation so stale completion cannot publish or remove a newer frame",
  "Successful commit/frame-ready publication is atomic, minimum event capacity holds one undrained start plus the two-event success, and saturation preserves the prior published frame",
  "The boundary explicitly excludes windows, tabs, input, history, asynchronous transport, process serialization, and Firefox UI parity",
]
evidence = [
  "22 locked tests passed: 5 navigation unit tests, 15 deterministic facade integration tests, and 2 existing real EGL/WebRender static-pipeline tests",
  "Tests cover admission and publication atomicity, supersession, cancellation, queue pressure, stale leases, frame limits, receiver drop, identity exhaustion, worker affinity, stable repeat shutdown, and contained factory/execution/shutdown/destructor failures and panics",
  "Independent post-fix review returned GO after the exact minimum-three event-capacity proof and explicit caught executor destruction were added",
  "Locked all-target check and strict Clippy passed on x86_64-unknown-linux-gnu from an external task-owned target directory",
  "W2-A6C now publishes one W2-A4D-composed zero-pending frame through this facade; there is still no window or UI",
  "docs/handoffs/W2-A6N-navigation-facade.md",
]

[[wave2.work_item]]
id = "W2-A6C"
owner = "agent-6-product-ui-tooling"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = ["browser/wild_buzzard_engine"]
acceptance = [
  "Only the canonical finalized scene text inventory is shaped after layout; speculative measurement allocations remain confined to the bounded text cache",
  "Every finalized entry, including whitespace, retains its exact Arc<ShapedText> and resolves in canonical order against the original compiled scene",
  "Layout projects first_baseline above and height minus first_baseline below the baseline, and the renderer adds fragment top exactly once",
  "One render_composed call submits page primitives, fonts, all positioned glyphs, epoch, and frame generation in one transaction and successful output has zero pending text",
  "The navigation facade publishes exactly that composed RgbaFrame through the current generation-tagged one-shot lease",
]
evidence = [
  "Independent source-freeze review returned GO with no high- or medium-severity findings",
  "24 locked tests passed after the post-review evidence regressions: 6 unit, 15 navigation facade, and 3 real static pipeline tests",
  "Strict no-dependency all-target Clippy, exact-file rustfmt, diff, manifest/lock, artifact, unsafe/FFI, provider, platform, and Firefox-dependency audits passed",
  "Deterministic numeric-loopback fixtures cover two positioned text blocks, exact canonical ordering, first-baseline rejection, repeated identical pixels, no-text, whitespace-only text, and real worker lease publication",
  "docs/handoffs/W2-A6C-composed-pipeline.md",
]

[[wave2.work_item]]
id = "W2-A2E"
owner = "agent-2-js-wasm"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = ["js"]
acceptance = [
  "Fresh generation-checked Symbol primitives preserve absent versus exact UTF-16 descriptions without exposing identity handles",
  "String and Symbol PropertyKeys share descriptor, lookup, definition, deletion, rooting, and tracing paths",
  "Ordinary objects and Arrays enumerate indices, strings, implicit Array length, and Symbols in the required stable category order",
  "Object.getOwnPropertyNames, Object.getOwnPropertySymbols, and Reflect.ownKeys share one bounded validated own-key source",
]
evidence = [
  "Rust-native Symbol arena and ordered unified PropertyKey storage integrated without dependencies or unsafe code",
  "100 JavaScript tests passed, including focused identity, exact-description, descriptor, ordering, Array-boundary, embedding, cross-realm-rejection, and GC regressions",
  "Independent audit found no blocking correctness or safety issue and corrected the documented conforming behavior of the ordinary Symbol prototype object",
  "Exact-file formatting, package all-target/all-feature check, strict Clippy, locked tests, release build, and warning-denied rustdoc passed on x86_64-unknown-linux-gnu using external targets",
  "docs/handoffs/W2-A2E-javascript-symbols.md",
]

[[wave2.work_item]]
id = "W2-A2G"
owner = "agent-2-js-wasm"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "in-progress"
write_paths = ["js/brimstone", "future Wild Buzzard Brimstone host/JIT adapter paths under js"]
acceptance = [
  "Exact latest-selected Brimstone source snapshot and MIT license are present with reproducible provenance and no nested repository metadata",
  "The snapshot builds and its honest upstream tests run independently outside the repository before any adaptation",
  "Unsafe Context/handle ownership and moving-GC rooting blockers are isolated and converted into executable hardening gates",
  "A bounded W^X Cranelift baseline-JIT prototype compiles hot arithmetic, control flow, calls, and cached property access while forced collection at every safepoint remains correct",
  "A browser-owned Wasmtime integration design proves JS/Wasm values, cross-heap roots, memories, tables, exceptions, jobs, interrupts, and error mapping without WASI exposure",
]
evidence = [
  "Exact Brimstone b544eff original import plus bfb720f current upstream refresh and selected green harness completed; docs/handoffs/W2-A2G-brimstone-import.md and docs/handoffs/W2-A2R-brimstone-refresh.md",
  "Brimstone owned-context/root-scope safety gate, heap teardown/resize fixes, focused clean ASan/LSan, and forced-GC evidence completed; docs/handoffs/W2-A2H-brimstone-safety.md",
  "Wasmtime v47.0.3 minimal-core audit passed product cargo check and recorded the upstream minimal-feature unit-test compile gap; docs/handoffs/W2-A2W-wasmtime-admission.md",
  "Exact Wasmtime v47.0.3 superproject and pinned core specification-suite source imported and locked minimal embedding smoke passed; docs/handoffs/W2-A2X-wasmtime-import.md",
  "Contained off-by-default Brimstone verifier/W^X/Cranelift proof completed; docs/handoffs/W2-A2J-brimstone-jit-infra.md",
  "Contained W2-A2K GC-linked native-frame/NewObject/Neg+Ret continuation proof completed with product dispatch compile-time false; docs/handoffs/W2-A2K-brimstone-gc-jit.md",
  "Contained W2-A2L rooted exact-function side exit resumes numeric Neg/Ret or uncaught terminal Throw in an actual Brimstone VM frame; product dispatch remains false; docs/handoffs/W2-A2L-brimstone-vm-continuation.md",
  "Contained W3-A2M bounded local-CFG continuation breadth executes in the actual Brimstone VM with polled backedges, exact cleanup, and product dispatch still false; docs/handoffs/W3-A2M-brimstone-vm-breadth.md",
  "W2-A2Y adds the first independently locked capability-free Wasmtime adapter; docs/handoffs/W2-A2Y-wasmtime-adapter.md",
  "Pending Brimstone internal lifetime completion and browser limits, broader native generation and helpers/calls/properties/handled exceptions, product JIT dispatch and optimizing tiers, complete JS/Wasm API and cross-heap bridge, Wasm specification/WPT execution, full Test262 accounting, and browser integration tests",
]

[[wave2.work_item]]
id = "W2-A2J"
owner = "agent-2-js-wasm"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = ["js/brimstone"]
acceptance = [
  "The baseline_jit feature is off by default, Linux x86-64 only, and compile-time disconnected from product VM dispatch",
  "All 151 opcodes have explicit use/def/effect/control-flow metadata and trusted compiler bytecode passes a bounded checked decoder/verifier before code generation",
  "Executable mappings are owner-thread, hard-budgeted, and transition RW to RX without an RWX phase",
  "Exact local Cranelift 0.134.3 emits and executes a bounded non-allocating boxed arithmetic/control-flow subset with exact side exits",
  "Documentation explicitly denies GC visibility, helper calls, native safepoints, interpreter resume, property fast paths, untrusted-bytecode safety, or product-JIT completion",
]
evidence = [
  "23 default, 45 baseline_jit, 46 combined forced-GC, 45 release, and 45 AddressSanitizer/LeakSanitizer brimstone_core library tests passed",
  "Strict Clippy passed for default and baseline_jit; local Cranelift/feature-off dependency-tree and prohibited-pattern audits passed",
  "Refreshed bfb720f integration harness passed 185/0/0 normally and 179/0 with six configured GC-stress skips; the new primitive-prototype regression passed directly",
  "Independent frozen-diff audit completed after correcting strict-equality allocation metadata and aligning the Cranelift registry closure",
  "docs/handoffs/W2-A2J-brimstone-jit-infra.md",
]

[[wave2.work_item]]
id = "W2-A2K"
owner = "agent-2-js-wasm"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = ["js/brimstone"]
acceptance = [
  "The baseline_jit feature remains off by default and PRODUCT_DISPATCH_ENABLED remains compile-time false",
  "One compiler-created artifact is consumed into a privately constructed cache entry which inseparably owns executable code, immutable safepoint metadata, and the exact verified decoded continuation program, including resolved constant-backed branch semantics",
  "A thread-affine higher-ranked activation validates opaque initialized value slots against the exact owning Context before linking a canonical native frame into its root chain",
  "Exact CFG liveness publishes every live initialized value before the sole allocating zero-argument NewObject helper, interruption is polled before allocation, and forced moving collection preserves and rewrites live roots and the rooted result",
  "A contained continuation resumes only exact Neg and Ret behavior without allocating in Brimstone's moving JavaScript heap or replaying NewObject; unsupported operations and backedges remain fail-closed side exits",
  "Documentation denies full VM/interpreter integration, DOM or untrusted execution, product-tier completion, and closure of remaining raw-internal, exception, deoptimization, debugger, invalidation, limit, conformance, fuzz, or Miri gates",
]
evidence = [
  "54 baseline_jit, 55 combined forced-GC, 54 release, and 54 AddressSanitizer/LeakSanitizer brimstone_core library tests passed on the frozen source",
  "Default workspace tests passed with 23 core and 11 integration/snapshot tests; strict default and baseline_jit workspace/all-target Clippy passed",
  "The pinned selected integration/Test262 manifest passed 185/0/0 at Test262 227d905513f790dec90858d04ddf8cf81326706f; the refreshed baseline's prior GC-stress harness passed 179/0 with six configured skips",
  "Exact-file rustfmt and git diff checks passed; warning-denied baseline_jit brimstone_core rustdoc passed with only four documented pre-existing imported-baseline lint classes allowed",
  "Independent frozen-diff review returned GO for the contained off-by-default proof with no high- or medium-severity finding, while retaining NO-GO for product VM/browser/DOM dispatch and untrusted content",
  "Recorded non-blocking coverage gaps are distinct maps for two NewObject callsites, forced GC while overwriting a moving-pointer destination, and wide/extra-wide Neg continuation resume",
  "docs/handoffs/W2-A2K-brimstone-gc-jit.md",
]

[[wave2.work_item]]
id = "W2-A2X"
owner = "agent-2-js-wasm"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = ["js/wasmtime"]
acceptance = [
  "Exact Wasmtime v47.0.3 superproject source is present with mode/blob/path equality and no nested Git metadata",
  "The exact pinned core WebAssembly specification suite is materialized while Component Model and WASI gitlink payload exclusions are explicit",
  "The selected default-off browser-core feature graph is locked, contains no Git dependency, and excludes public WASI, component, Winch, and ambient host APIs",
  "A Linux x86-64 embedding compiles and instantiates binary Wasm without WAT or WASI",
]
evidence = [
  "6,859 exact superproject blobs plus 296 exact core spec-suite blobs and one Wild Buzzard provenance note",
  "Locked closure: 23 Wasmtime-tree packages, 59 registry packages, and zero Git dependencies",
  "External locked cargo check and empty binary-module compile/instantiate smoke passed",
  "docs/handoffs/W2-A2X-wasmtime-import.md",
]

[[wave2.work_item]]
id = "W2-A2L"
owner = "agent-2-js-wasm"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = ["js/brimstone"]
acceptance = [
  "baseline_jit remains off by default and PRODUCT_DISPATCH_ENABLED remains compile-time false",
  "A lifetime-branded binding roots the exact closure, function, scope, realm, constants, and caches and ties one never-reused identity to the loaded artifact across moving GC",
  "Admission rejects parameters, noninitial realms, runtime functions, handlers, ordinary value constants, and nonempty caches while exact raw jump descriptors are revalidated",
  "A native side exit clears dead slots, unlinks its activation, and resumes only through a private admitted proof in an actual Brimstone VM frame at the exact prefix-inclusive boundary",
  "Only numeric local Neg-to-Ret and uncaught terminal Throw tails are admitted, with fail-closed native/VM return, throw, interruption, allocation, poison, and exact stack-state cleanup",
  "Forced-GC, prefix, safepoint-map, overwrite, cleanup, capacity, recovery, static, sanitizer, and independent-review gates pass",
  "Documentation denies product dispatch, broad VM integration, DOM/untrusted execution, and baseline/optimizing-tier completion",
]
evidence = [
  "Frozen ordered source-hash aggregate 979fe0829ac920e586e16aae812793bed615b8b3e3f9f239959de63830a0067c across exactly 11 Brimstone files",
  "Owner gates passed 35 default and 80 baseline workspace tests; focused continuation, ABI, stack, allocation, moving-GC, release, strict Clippy, and external ASan/LSan gates passed",
  "Independent fresh-target gates passed 80 baseline workspace, 17 forced-GC/allocation, 69 release, and 17 ASan/LSan tests with no failure or sanitizer diagnostic",
  "Configured nightly and pinned formatting, diff, dependency, prohibited-pattern, stack-capacity, product-dispatch-false, and warning-denied core rustdoc gates passed; strict imported-workspace rustdoc debt remains explicit",
  "Independent review returned GO only for the contained disabled gate and retained NO-GO for product/DOM/untrusted dispatch",
  "docs/handoffs/W2-A2L-brimstone-vm-continuation.md",
]

[[wave2.work_item]]
id = "W2-A2Y"
owner = "agent-2-js-wasm"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = ["js/wasm"]
acceptance = [
  "An independently locked Rust 2024/MSRV-1.94 crate uses exact local Wasmtime v47.0.3 with defaults disabled and only std,runtime,cranelift,gc,gc-drc,threads",
  "One process Engine owns owner- and generation-checked modules, stores, and instances with deterministic invalidation and teardown",
  "Only bounded binary import-free modules and i32-only exports are admitted; WASI, WAT, Linker, host functions, component, cache, async/fibers, and native-code deserialization are absent",
  "Cranelift, fuel, epoch interruption, stack/resource limits, conservative resident-instance accounting, and poison-on-sequence-exhaustion fail closed",
  "Runtime Wasm GC, threads/shared memory, memory64, stack switching, and other gated proposals remain disabled",
  "Documentation explicitly denies total-RSS accounting, compilation cancellation/code accounting, JavaScript WebAssembly API, Brimstone/cross-heap integration, conformance, sandbox, AppImage, or product activation",
]
evidence = [
  "Owner gate GO: 109 lock records with all 108 non-root records identical to the audited seed; selected Linux graph is 1 adapter + 23 Wasmtime-tree + 59 registry packages and zero Git dependencies",
  "Owner locked offline check, exception-free strict all-target Clippy, 27 tests, release build, and warning-denied rustdoc passed under /home/user/Documents/wildbuzzardbuilds/w2-a2y-wasm-adapter/target-owner",
  "Independent fresh-target review reproduced the exact lock, graph, strict Clippy, 27 tests, release, warning-denied rustdoc, capability/unsafe/product-disconnection, and artifact-containment gates under /home/user/Documents/wildbuzzardbuilds/w2-a2y-final-review/target",
  "Frozen aggregate 7c40e5355a81a4aef8c2431d6b4640e0c0d7fdeb384ffc3eace5972a5ae4dc5f; source-only d935b51df8c2214ff397e7e735b8d760f238b38b69f456e93e2054886dabf8a3; lock 83a227c8d00fd79a047f50a2aa44684cb13344d9440e4586ca0e86c0c160efae",
  "Independent GO applies only to the contained product-disconnected adapter; JS WebAssembly, cross-heap, total-resource, conformance, sandbox, AppImage, and untrusted execution remain NO-GO",
  "docs/handoffs/W2-A2Y-wasmtime-adapter.md",
]

[[wave3.work_item]]
id = "W3-A3S"
owner = "agent-3-dom-style-layout"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = ["dom/src/bindings.rs", "dom/tests/script_mutation.rs"]
acceptance = [
  "One nonempty bounded batch requires an exact DocumentVersion and supports eight initial DOM mutation forms",
  "Existing NodeIds are document-checked and batch-created nodes use dense local tokens which may refer only backward",
  "Every rejection leaves the tree, revision, and node-slot allocation unchanged; one success validates and publishes exactly one new revision and snapshot",
  "NodeId remains a lookup identity rather than a GC root and no concrete JavaScript heap dependency enters DOM",
  "Documentation denies Brimstone binding, event loop, MutationObserver delivery, incremental invalidation, scalability, and DOM parity",
]
evidence = [
  "Focused transaction suite passed 11 tests and the complete DOM package passed 24 tests",
  "Locked package check, strict all-target Clippy, release build, warning-denied no-dependency rustdoc, exact formatting, and diff checks passed using an external target",
  "Frozen bindings hash b7236e3893e973fac22491653c1778e65749a4c3e824ec2e1f749c5b42748b05 and test hash 5dcf986a20f4dd049a46f1d071b80b7b556a09462f454066958f4cbc9616b6ae",
  "docs/handoffs/W3-A3S-script-mutation.md",
]

[[wave3.work_item]]
id = "W3-A2M"
owner = "agent-2-js-wasm"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = ["js/brimstone/src/js/runtime/bytecode/vm.rs", "js/brimstone/src/js/runtime/jit/continuation.rs", "js/brimstone/src/js/runtime/jit/mod.rs"]
acceptance = [
  "The off-by-default private actual-VM continuation is admitted by a fallible bounded monotone CFG/type proof from the exact side exit",
  "Local moves/immediates, valid-JS logical/type operations, number-only arithmetic/comparisons, exact branch families, joins, loops, Ret, and uncaught terminal Throw are covered",
  "Every taken nonpositive edge verifies and publishes its exact target before deterministic interrupt polling, with exact parent VM cleanup on every terminal path and panic",
  "Generated native breadth remains the earlier tiny subset, product dispatch remains compile-time false, and DOM or untrusted-page entry remains prohibited",
]
evidence = [
  "Focused baseline tests passed 24 cases; combined forced-GC/allocation/handle tests and independent ASan/LSan passed 25 cases",
  "Default workspace passed 35 tests and baseline workspace passed 88 tests; strict default, baseline, and combined-feature Clippy, release, formatting, diff, and warning-denied allowlisted core rustdoc gates passed",
  "Independent frozen-source audit returned GO for the contained product-disconnected proof and retained NO-GO for product dispatch",
  "Frozen ordered three-file hash aggregate 30bbfee45f0b92ecd1982adeca2c72383458e8e351956ac2075a3374564c14ac",
  "docs/handoffs/W3-A2M-brimstone-vm-breadth.md",
]

[[wave3.work_item]]
id = "W3-A6W"
owner = "agent-1-foundation-platform"
model = "gpt-5.6-sol"
reasoning_effort = "high"
state = "complete"
write_paths = ["widget/rust/wild_buzzard_linux"]
acceptance = [
  "A first-party unsafe-free Linux x86-64 event shell selects exact winit 0.30.13 with defaults off and Wayland/X11 only",
  "Lifecycle, event admission, callback-requested stop, permanent wake closure, scale-versus-resize semantics, and normal terminal ordering are deterministic",
  "Opt-in real-display smokes verify the selected Wayland and X11 backends without claiming a renderer surface or browser UI",
  "Dependency provenance, dynamically loaded library boundary, AppImage work, and evidence limitations are explicit",
]
evidence = [
  "Owner gates passed formatting, locked all-target check, 26 tests, project-defined strict Clippy, release, warning-denied rustdoc, doctests, and forced Wayland/X11 live-display smokes",
  "Independent fresh-target review repeated 26 tests, strict project Clippy, and both live-display smokes and returned GO for the event-shell scope",
  "Exact winit checksum a6755fa58a9f8350bd1e472d4c3fcc25f824ec358933bba33306d0b63df5978d and upstream v0.30.13 peeled revision e9809ef54b18499bb4f2cac945719ecc2a61061b are recorded",
  "Browser presentation, native-handle/surface integration, AppImage closure, complete suppressed-event accounting, callback-panic terminal delivery, internal smoke timeout, and pedantic-clean status remain open",
  "docs/handoffs/W3-A6W-linux-window.md",
]

[[wave3.work_item]]
id = "W3-A6D"
owner = "agent-6-browser-product"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = ["browser/wild_buzzard_engine", "gfx/wild_buzzard_headless/src/headless.rs", "gfx/wild_buzzard_headless/tests/composed_scene.rs"]
acceptance = [
  "A successful load retains exactly one opaque live document; L is its exact revision and F is the revision represented by the last frame successfully returned",
  "Precommit mutation rejection is atomic; after a successful batch the DOM commit is irreversible and a downstream failure reports the advanced L, unchanged F, and exact created-node map",
  "Exact-version rerender performs no fetch, parse, mutation, created-node mapping, or revision increment",
  "A successful render has no later fallible checkpoint; false renderer health is terminal and true predicts no success; render epochs are monotone attempt IDs and may gap",
  "Documentation denies Brimstone/script/event-loop/navigation-facade integration, live invalidation, cumulative document accounting, untrusted-page permission, window presentation, and parity",
]
evidence = [
  "Owner gates passed 8 focused and 33 full nested-workspace tests, locked Linux all-target check, strict all+pedantic no-dependency Clippy, release, warning-denied no-dependency rustdoc, formatting, and diff checks",
  "Owner outputs remained under /home/user/Documents/wildbuzzardbuilds/w3-a6d-dynamic-document-corrected",
  "Independent hostile review returned GO for the contained state-machine scope with no high/medium finding and repeated 8 focused tests, strict pedantic Clippy, unusable preflight, and real post-send poison/teardown under an external fresh target",
  "Post-review wording-only correction received independent GO; final dynamic.rs hash d02bb3ffad8a7a86130ffaa726c69a5f55efee5f7abf68d6353911d336c8acd1 and engine README hash 61da0771d689f4ad389dab4b6faf7aea6baaf2c3d26cf4d1c949672641f770d8",
  "Product/browser acceptance remains NO-GO: no JS/task/navigation-facade/presentation activation, live invalidation, cumulative document accounting, Firefox/WPT parity, or untrusted-script permission",
  "docs/handoffs/W3-A6D-dynamic-document.md",
]

[wave4]
id = "W4"
name = "native execution and live Linux integration prerequisites"
state = "complete"
integration_goal = "Broaden the disabled canonical Brimstone native path, expose bounded live-document operations through the worker, and prove a fail-closed direct Linux EGL window surface without claiming page-script or WebRender-window product activation."
tasks = [
  "Agent 2 complete for W4-A2N: bounded native SMI operations, exact branches/joins, polled loops, GC-safe NewObject, Ret, and rooted actual-VM side exits; product dispatch remains compile-time false",
  "Agent 4 complete for W4-A4P: reviewed checked-EGL direct-GL window presenter with exact startup/teardown outcomes and live Wayland/X11 swap-submission smokes; no WebRender or desktop-compositor acknowledgement",
  "Agent 6 complete for W4-A6E: bounded multi-context mutation/rerender/close and engine-incarnation-bound document-operation cancellation passed 33 focused and 54 full tests plus a clean final hostile rereview",
]
integration_evidence = [
  "A fresh external root target passed locked Linux all-target workspace check, the full root test suite including 27 presenter and 27 Linux-shell tests plus the presenter compile-fail doctest, and the optimized workspace release build",
  "Exact-file formatting, strict warning-denied Clippy, and warning-denied no-dependency rustdoc passed for the changed root packages; whole-workspace formatting/lint/doc strictness remains limited by pre-existing imported qcms, WebRender, prefs-parser, and skv debt rather than W4 paths",
  "The final nested browser rerun passed locked all-target check, 33 focused and 54 full tests, strict all-plus-pedantic Clippy, warning-denied rustdoc, and optimized release using external output",
]

[[wave4.work_item]]
id = "W4-A2N"
owner = "agent-2-js-wasm"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = [
  "js/brimstone/src/js/runtime/jit/abi.rs",
  "js/brimstone/src/js/runtime/jit/compiler.rs",
  "js/brimstone/src/js/runtime/jit/continuation.rs",
  "js/brimstone/src/js/runtime/jit/mod.rs",
]
acceptance = [
  "baseline_jit remains off by default and PRODUCT_DISPATCH_ENABLED remains compile-time false",
  "Generated native code covers bounded immediate/move and SMI arithmetic, bitwise, shift, unary, comparison, exact branch, join, loop, NewObject, and Ret families with exact rooted slow exits",
  "Must-provenance analysis prevents Empty or internal pointer-shaped VM metadata from influencing native JavaScript return or undefined/nullish control flow",
  "Every taken nonpositive native edge publishes its exact target before a versioned helper poll and an independent one-million-edge cap fails closed",
  "Allocating and poll callsites have disjoint locations, exact emitted-call accounting, and native reachability stops at mandatory side exits",
  "Moving-GC, interrupt, panic/poison, parent-state cleanup, strict, release, rustdoc, and sanitizer gates pass",
]
evidence = [
  "35 default and 101 baseline-JIT full-workspace tests passed; 66 focused forced-GC/allocation/handle JIT tests passed",
  "Strict default, baseline, and combined-feature all-target Clippy, release, exact rustfmt, diff, and warning-denied allowlisted core-rustdoc gates passed",
  "Nightly AddressSanitizer plus LeakSanitizer passed all 66 focused tests with forced moving GC and no diagnostic",
  "Independent hostile read-only review found no Critical, High, Medium, or Low correctness defect and reproduced all four frozen source hashes",
  "docs/handoffs/W4-A2N-native-jit-cfg.md",
]

[[wave4.work_item]]
id = "W4-A4P"
owner = "agent-4-graphics-media"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = [
  "gfx/wild_buzzard_linux_presenter",
  "widget/rust/wild_buzzard_linux",
]
acceptance = [
  "One synchronous preparation transaction preserves exact display/window/context/surface affinity and selects hardware-only RGBA8/A8 sRGB desktop EGL for Wayland or X11",
  "Checked raw eglQuerySurface proves canonical success, positive dimensions, and exact extent after create/resize and before each direct frame",
  "Initialized diagnostic readback and the first authoritative GL/native fault override renderer swallow, remap, or panic before a swap receipt can publish",
  "Startup and runtime teardown distinguish no owner, checked wrapper release, and fail-closed retention without claiming native EGL destruction",
  "A receipt proves only exact direct-frame completion and accepted EGL swap submission, never WebRender presentation or desktop-compositor display",
]
evidence = [
  "54 unit tests (27 shell and 27 presenter) plus one compile-fail doctest passed",
  "Locked focused and root all-target checks, strict package Clippy, warning-denied no-dependency rustdoc, release, exact rustfmt, diff, and ELF/RPATH audits passed",
  "Fresh checked-query live smokes passed on Wayland in 1.27 seconds and X11/Xwayland in 1.28 seconds",
  "Independent final hostile rereview found no Critical, High, Medium, or Low defect after checked-extent and partial-startup-teardown repairs",
  "docs/handoffs/W4-A4P-linux-presenter.md",
]

[[wave4.work_item]]
id = "W4-A6E"
owner = "agent-6-browser-product"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = ["browser/wild_buzzard_engine"]
acceptance = [
  "The worker retains independent opaque pages per bounded context and names mutation, rerender, and close with exact navigation/document identities",
  "Retained plus pending document-node capacity is checked across contexts before navigation publication and executor entry, then converted atomically at mutation commit",
  "Successful mutation allocation maps derive from an opaque DOM-issued commit and pass exact topology, version, document, cardinality, and uniqueness validation",
  "Close permanently retires a context identity and navigation-only replacement retires prior typed state, charges, frames, and result leases",
  "Each document operation receives an engine-owned never-reused operation identity; exact operation cancellation rejects restored-page, sequential-operation, cross-engine, supersession, close, and exhaustion ABA cases",
  "Documentation denies Brimstone/script tasks, DOM events/microtasks, incremental invalidation, complete RSS accounting, presentation, browser UI, and untrusted-page activation",
]
evidence = [
  "The final owner gate passed 33 focused navigation-facade and 54 full nested-workspace tests (10 library, 8 dynamic-document, 33 navigation-facade, and 3 static-pipeline)",
  "Package formatting, locked all-target workspace check, strict all+pedantic Clippy, warning-denied no-dependency rustdoc, optimized release, and diff checks passed using external output",
  "The earlier hostile review found the four original accounting/identity/proof/retirement repairs sound and identified the operation-level cancellation ABA now repaired with DocumentOperationId",
  "Final hostile read-only rereview found no Critical, High, Medium, or Low defect and confirmed the 33 focused / 54 full test evidence",
  "docs/handoffs/W4-A6E-dynamic-navigation.md",
]

[wave5]
id = "W5"
name = "bounded product and native-window connection prerequisites"
state = "complete"
integration_goal = "Connect the disabled Brimstone native proof to an ordinary test-only VM call boundary, render a typed immutable scene directly through WebRender into the Linux EGL surface, and add nonvisual browser-session state without claiming a runnable browser, page script, compositor display, or parity."
tasks = [
  "Agent 2 complete for W5-A2O: test-only ordinary Rust-call hot dispatch, exact stack/realm/root/cache/unwind contracts, and inline ordinary backedge polling; product dispatch remains compile-time false",
  "Agent 4 complete for W5-A4Q: same-process WebRender renders one exact immutable scene directly into the Wayland/X11 EGL surface with no normal-path CPU frame copy; no browser connection or compositor acknowledgement",
  "Agent 6 complete for W5-A6F: independently locked nonvisual browser-session controller with bounded window/tab/address/history state, exact navigation phases, monotone live publication, engine leases, input routing, and shutdown",
]
integration_evidence = [
  "All W5 Cargo, rustc, rustdoc, sanitizer, and live-smoke output remained outside the repository under /home/user/Documents/wildbuzzardbuilds",
  "Independent hostile final audits returned GO for the contained W5-A2O, W5-A4Q, and W5-A6F scopes after ownership, teardown, event-wake, stale-cache, realm, VM unwind, navigation rollback, and documentation corrections",
  "Runnable-browser, normal-site, YouTube, Firefox engine/UI parity, product JIT dispatch, browser-chrome presentation, and AppImage claims remain false",
]

[[wave5.work_item]]
id = "W5-A2O"
owner = "agent-2-js-wasm"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = [
  "js/brimstone/src/js/runtime/bytecode/function.rs",
  "js/brimstone/src/js/runtime/bytecode/vm.rs",
  "js/brimstone/src/js/runtime/context.rs",
  "js/brimstone/src/js/runtime/gc/handle.rs",
  "js/brimstone/src/js/runtime/jit",
]
acceptance = [
  "baseline_jit remains off by default and PRODUCT_DISPATCH_ENABLED remains compile-time false",
  "The sole contained hot hook is the ordinary exact-arity Rust-to-bytecode VM branch under test-only admission, with exact push-equivalent stack/depth and caller/callee/initial-realm preflight before effects",
  "Generation-checked moving roots, bounded positive/negative RX cache entries, exact receiver/formal capture, and whole-entry no-replay admission stay coherent across moving GC and teardown",
  "Every Rust-caller VM frame and handle scope restores exact parent state on terminal unwind; stale cache/artifact disagreement aborts fail-closed",
  "Every ordinary taken native backedge polls a stable inline atomic state without entering Rust unless a slow condition exists",
]
evidence = [
  "35 default and 120 baseline-JIT full-workspace tests passed; the unfiltered handle-stat suite passed 112 tests and the combined forced-GC/allocation/handle JIT suite passed 89",
  "Default, baseline, and combined strict all-target Clippy, warning-denied release check, allowlisted warning-denied core rustdoc, exact rustfmt, diff/policy/artifact audits, and final refreshed suites passed",
  "Nightly AddressSanitizer plus LeakSanitizer passed all 89 focused tests with forced moving GC and no diagnostic",
  "Independent final audit reproduced all 11 source hashes and returned GO while retaining NO-GO for product, DOM, or untrusted-page dispatch",
  "docs/handoffs/W5-A2O-brimstone-hot-dispatch.md",
]

[[wave5.work_item]]
id = "W5-A4Q"
owner = "agent-4-graphics-media"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = [
  "gfx/wild_buzzard_linux_presenter",
  "gfx/wild_buzzard_renderer/src/compiler.rs",
  "gfx/wild_buzzard_renderer/src/contract.rs",
]
acceptance = [
  "One internally owned WebRender renderer consumes the exact immutable CompiledScene/pipeline/text inventory and renders directly to framebuffer zero on the exact current EGL surface",
  "Scene/document/surface revision/extent/epoch/swap identities, bounded transaction size, checkpoints, deadlines, and native state are validated before publication",
  "The normal path has no CPU readback, frame copy, screenshot upload, second renderer, or exported GL/EGL/native authority",
  "Sticky external renderer events, contradictory native state, timeout, driver/renderer faults, and unprovable teardown fail closed with typed evidence",
  "A receipt proves WebRender completion and EGL swap submission only, never desktop-compositor display or browser/UI connection",
]
evidence = [
  "75 default presenter tests and 51 WebRender-window feature tests plus the example/compile-fail coverage passed",
  "Locked checks, strict Clippy, warning-denied rustdoc/doctests, release, formatting, dependency/ELF audits, and exact frozen hashes passed",
  "Fresh Wayland and X11/Xwayland live smokes used the same binary and exited successfully without CPU readback/upload",
  "Two hostile review cycles and final rereview returned GO after suspend-scale, teardown, deadline, early-validation, and sticky-wake corrections",
  "docs/handoffs/W5-A4Q-webrender-window.md",
]

[[wave5.work_item]]
id = "W5-A6F"
owner = "agent-6-browser-product"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = [
  "browser/wild_buzzard_engine/src/navigation.rs",
  "browser/wild_buzzard_ui",
]
acceptance = [
  "The concrete port owns an inseparably spawned engine/receiver pair and validates sequenced one-shot frame/result transfer without cross-incarnation construction",
  "A bounded session-wide phase ledger distinguishes latest, loading, and live navigations; prior live state survives pending replacement and successful publication is generation-monotone",
  "Initial and dynamic document/frame/result versions and leases are exact even when pixels become stale; old pixels are never relabeled",
  "Window/surface reverse identity, close tombstones, aggregate history/frame accounting, and post-sequence terminal cleanup fail closed",
  "Shutdown releases receiver-owned queues/leases/document metadata/accounting before potentially unbounded join and retains no engine owner after return",
]
evidence = [
  "55 engine tests and 54 UI tests passed, including real-worker races, hostile generic-port rollback, lifecycle, resource, lease, document-version, panic, and shutdown cases",
  "Engine and UI formatting, locked all-target check, strict all-target Clippy, warning-denied no-dependency rustdoc, doctests, and the compile-fail cross-pair API proof passed",
  "Independent final audit reproduced all 11 source hashes and returned GO with no Critical, High, Medium, or Low finding",
  "The crate has no executable, rendered chrome, presenter connection, page-input route, public mutation admission, persistence, process isolation, or parity claim",
  "docs/handoffs/W5-A6F-browser-session.md",
]

[wave6]
id = "W6"
name = "script-call and runnable Rust browser-shell connection"
state = "complete"
integration_goal = "Run one bounded page through the retained navigation/session state into an actual Rust Wayland/X11 browser executable that composes page content and Rust chrome directly with WebRender, while deepening the disabled canonical Brimstone tier through ordinary in-VM calls and constructors."
tasks = [
  "W6-A2P complete: canonical Brimstone ordinary in-VM call/construct execution with exact arguments, receiver/new-target, moving-GC, unwind, interrupt, and no-replay semantics; product dispatch remains compile-time false",
  "W6-A4R complete: capability-safe page-plus-Rust-chrome WebRender composition on the exact native EGL surface with direct framebuffer output and deterministic hit identity",
  "W6-A6G complete: independently locked Rust executable connecting BrowserSession, NavigationEngine scene leases, Linux events/input, and the W6-A4R presenter API",
]
integration_evidence = [
  "The W5 commit and origin/main were reverified at 4f2c83ade33ee26eb6d0f6a8afabd9a4849c1fc6 before W6 started",
  "All nine pre-existing user-owned js/README.md, js/src, and js/tests worktree hashes were reproduced unchanged and remain outside every W6 owner write scope and commit",
  "Independent hostile review and integration audit returned GO after repairing exact shutdown, stale hit, rerender liveness, lifecycle overlap, WebRender pipeline-removal, synchronous Wayland resize, and smoke-watchdog defects",
  "The same release wild-buzzard binary completed the exact loopback smoke on Wayland with its success marker at composition 12 and final requested shutdown at 58, and on X11 with its success marker at 13 and final requested shutdown at 84",
  "Normal-site, YouTube, product-JIT, desktop-compositor-display, Firefox parity, and AppImage claims remain false",
]

[[wave6.work_item]]
id = "W6-A2P"
owner = "agent-2-js-wasm"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = [
  "js/brimstone",
  "docs/handoffs/W6-A2P-brimstone-in-vm-calls.md",
]
acceptance = [
  "Ordinary JavaScript-to-JavaScript nested, recursive, and constructor bytecode paths can enter or remain in the contained native tier with canonical missing/extra argument, receiver, closure, and new-target behavior",
  "Every generated helper/call boundary publishes exact PC and GC-visible state, reloads moving references, preserves throw/interrupt priority, never replays committed effects, and restores exact VM/handle/frame/realm state",
  "Stack/depth/resource/code/root/cache bounds and stale identity fail closed across recursion, allocation, panic, eviction, and context reuse",
  "baseline_jit remains off by default and PRODUCT_DISPATCH_ENABLED remains a literal compile-time false; DOM and untrusted-page admission remain impossible",
  "Rust-host callback reentry while an outer dispatch capability is borrowed falls back to the canonical interpreter; callback tier reentry is deferred rather than represented by an aliased raw dispatcher",
]
evidence = [
  "35 default, 120 baseline-JIT, 124 handle-stat, and 127 combined stress tests passed",
  "102 focused ASan/LSan tests passed with no address or leak diagnostic",
  "Default/nondefault policy, strict Clippy, release, rustdoc, formatting, diff, and exact-source audits passed",
  "docs/handoffs/W6-A2P-brimstone-in-vm-calls.md",
]

[[wave6.work_item]]
id = "W6-A4R"
owner = "agent-4-graphics-media"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = [
  "gfx/wild_buzzard_linux_presenter",
  "gfx/wild_buzzard_renderer",
  "docs/handoffs/W6-A4R-browser-compositor.md",
]
acceptance = [
  "One renderer-owned WebRender document composes independently revisioned page content and Rust chrome with exact z-order, clipping, scale, surface, epoch, swap, and hit-test identities",
  "The native-window API exports no GL/EGL/winit/raw-handle/renderer authority and the normal path performs no CPU frame readback, copy, upload, or second render",
  "Resize, suspend, wake/deadline races, stale scene combinations, resource bounds, device/native failure, and teardown fail closed without relabeling old pixels",
  "Fresh Wayland and X11 evidence uses the same binary and proves only WebRender completion plus EGL swap submission, not desktop-compositor acknowledgement",
]
evidence = [
  "71 presenter tests, warning-denied Clippy, formatting, metadata, doctest/rustdoc, and release gates passed after the pipeline-removal repair",
  "Exact WebRender PipelineInfo validates root/chrome/page epochs plus the retired-page removal tuple without treating the historical current_epoch cache as live reachability",
  "Fresh live Wayland and X11 evidence passed through the integrated same release binary without CPU frame readback/copy/upload",
  "docs/handoffs/W6-A4R-browser-compositor.md",
]

[[wave6.work_item]]
id = "W6-A6G"
owner = "agent-6-browser-product"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = [
  "browser/wild_buzzard_engine",
  "browser/wild_buzzard_ui",
  "browser/wild_buzzard_shell",
  "widget/rust/wild_buzzard_linux",
  "docs/handoffs/W6-A6G-rust-browser-shell.md",
]
acceptance = [
  "A real Rust executable owns the exact session/engine/receiver/window/presenter graph and displays retained page content plus Rust-authored tab/address chrome",
  "Address, tab, focus, close, resize, scale, redraw, suspend, and deterministic pointer/keyboard chrome actions route through bounded typed contracts",
  "The shell consumes an immutable navigation/document/scene lease rather than copying or relabeling headless pixels, and retains the old live page until replacement publication",
  "Same-executable Wayland and X11 loopback smokes and exact teardown pass without claiming general networking, page JavaScript/input, accessibility, media, AppImage, or parity",
]
evidence = [
  "56 engine, 56 UI, 32 widget, and 25 shell tests passed with strict owned-code Clippy, formatting, documentation, and release gates",
  "The same release binary passed Wayland with its success marker at composition 12 and final requested shutdown at 58, and X11 with its success marker at 13 and final requested shutdown at 84; both proved exact page clear/removal/restore and clean ordered teardown",
  "Synchronous winit resize results now enter the canonical presenter transition after callback return, and unfinished smoke stages retain watchdog wake admission",
  "Independent A4 cross-review and final integration audit returned GO",
  "docs/handoffs/W6-A6G-rust-browser-shell.md",
]

[wave7]
id = "W7"
name = "rooted runtime breadth and functional Firefox-like primary chrome"
state = "complete"
integration_goal = "Add rooted non-SMI constants and ordinary closure creation to the disabled canonical Brimstone tier while replacing the bounded W6 chrome with the first functional Firefox-ESR-shaped primary toolbar and panel slice on the same Rust/WebRender/Linux path."
tasks = [
  "W7-A2Q complete: rooted typed constant-table admission plus ordinary closure creation in the canonical Brimstone VM/JIT continuation, still side-exiting native execution where required and keeping product dispatch false",
  "W7-A4S complete: typed Firefox-like primary toolbar and panel scene/hit contract with responsive scale/overflow/RTL behavior and no Mozilla artwork",
  "W7-A6H complete: functional back/forward/reload-stop/identity/address/new-tab/all-tabs/application-menu state and actions, keyboard/pointer routing, popup scrolling, and live Wayland/X11 evidence",
]
integration_evidence = [
  "W7 is the reviewed integration checkpoint; the full-browser goal remains active and W8 has started without a pause",
  "The read-only Firefox ESR153 checkout and relevant browser.xhtml, navigator-toolbox, customizableui, tabbrowser, URL-bar, and theme tests are behavioral references only",
  "Wild Buzzard branding, provider-neutral defaults, Linux x86-64-only scope, external build targets, and protected user-owned JavaScript worktree files remain mandatory",
  "Independent frozen-source matrices passed 71 UI, 34 widget, and 33 integrated-shell tests; strict Clippy, rustdoc, formatting, TOML, and diff gates passed",
  "The frozen release binary passed owner Wayland 1/1 and X11 10/10 plus independent Wayland 5/5 and X11 10/10 normal-size runs with clean Requested exits",
  "Normal-site, YouTube, untrusted-page-script, desktop-compositor-display, Firefox parity, accessibility parity, AppImage, and release claims remain false",
]

[[wave7.work_item]]
id = "W7-A2Q"
owner = "agent-2-js-wasm"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = [
  "js/brimstone/src/js/runtime/bytecode/verifier.rs",
  "js/brimstone/src/js/runtime/jit/compiler.rs",
  "js/brimstone/src/js/runtime/jit/continuation.rs",
  "js/brimstone/src/js/runtime/jit/dispatch.rs",
  "docs/handoffs/W7-A2Q-brimstone-rooted-constants.md",
]
acceptance = [
  "A hard-bounded pointer-free typed descriptor table admits string, BigInt, non-SMI number, and other supported constants only after exact rooted identity revalidation around moving GC",
  "Ordinary NewClosure follows canonical synchronous VM semantics with exact realm/function/environment/root ownership and no replay across native side exit or continuation",
  "Malformed/stale/exhausted descriptors, GC relocation, interruption, throw, panic, cache eviction, and context reuse fail closed under focused stress and sanitizer tests",
  "baseline_jit remains nondefault, PRODUCT_DISPATCH_ENABLED remains literal false, and DOM/untrusted-page admission remains impossible",
]
evidence = [
  "35 default, 127 baseline, 131 handle-stat, and 134 stress tests passed",
  "Focused ASan/LSan passed 109 tests with no address or leak diagnostic",
  "Independent hostile review returned GO on the frozen four-file source set",
  "docs/handoffs/W7-A2Q-brimstone-rooted-constants.md",
]

[[wave7.work_item]]
id = "W7-A4S"
owner = "agent-4-graphics-media"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = [
  "gfx/wild_buzzard_linux_presenter",
  "docs/handoffs/W7-A4S-firefox-primary-chrome.md",
]
acceptance = [
  "One typed scene contract paints Firefox-like tabs/titlebar and primary toolbar controls for back, forward, reload/stop, identity, URL bar, new tab, all-tabs, overflow, and application menu using Wild Buzzard identity",
  "Enabled, pressed, loading, focus, selection, open-panel, hover, overflow, scale, narrow-window, and RTL states have deterministic layout, z-order, clipping, receipts, and hit identities",
  "Panels and controls expose no private renderer/native authority and preserve W6 exact epoch/removal/surface/teardown guarantees",
  "Deterministic scene tests plus same-binary live Wayland/X11 WebRender/EGL smokes demonstrate the same functional state supplied by Agent 6, without claiming desktop-compositor display or complete Firefox UI parity",
]
evidence = [
  "The final presenter matrix passed 96 tests after the checked native-extent seam was added",
  "Typed primary-chrome scene, popup, hit, overflow, scale, and RTL evidence received independent GO",
  "The normal-size 50/64-tab minimum-width overflow/scroll behavior remains an explicit follow-up",
  "docs/handoffs/W7-A4S-firefox-primary-chrome.md",
]

[[wave7.work_item]]
id = "W7-A6H"
owner = "agent-6-browser-product"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = [
  "browser/wild_buzzard_ui",
  "browser/wild_buzzard_shell",
  "widget/rust/wild_buzzard_linux",
  "docs/parity/browser-ui.toml",
  "docs/handoffs/W7-A6H-functional-primary-ui.md",
]
acceptance = [
  "Every visible W7 control is driven by canonical typed state and invokes its real session action; painted-only controls are rejected",
  "Back, forward, reload/stop, identity/address focus, new tab, all-tabs, application/overflow panel, close, and tab selection work through keyboard and primary-pointer paths with stale receipt protection",
  "Firefox ESR-shaped focus order, shortcut enablement, loading transitions, panel modality/dismissal, narrow-window overflow, scale, and RTL behavior have adapted tests and a recorded parity matrix",
  "The same release binary passes deterministic interaction smokes and live Wayland/X11 evidence with Wild Buzzard branding and honest non-claims for settings/bookmarks/history/downloads and later UI lanes",
]
evidence = [
  "Final locked matrices passed 71 UI, 34 widget, and 33 integrated-shell tests",
  "The frozen release SHA-256 ae1c1c7c7a5ddbf689c22da72424ffb639b0a946c8e9381d594ace1a339fc9a6 passed owner Wayland 1/1 and X11 10/10 plus independent Wayland 5/5 and X11 10/10",
  "The exact X11/Wayland checked-resize guard coalesces and reissues one deferred redraw only after verified publication, including unequal-duplicate and lifecycle regressions",
  "docs/parity/browser-ui.toml",
  "docs/handoffs/W7-A6H-functional-primary-ui.md",
]

[wave8]
id = "W8"
name = "normal-desktop web capability vertical"
state = "complete"
integration_goal = "Advance from the numeric-loopback browser slice toward ordinary desktop sites with bounded Brimstone browser admission, verified public-web DNS/HTTPS transport, and author-CSS layout behavior exercised by deterministic normal-size fixtures before opt-in live comparison."
tasks = [
  "W8-A2R complete after corrective hostile review: bounded classic-script admission and explicit microtask checkpoint in the adopted Brimstone context; untrusted pages remain NO-GO",
  "W8-A5 complete: separate general-web target, system-configured DNS, verified rustls TLS, and bounded HTTP/1.1 response delivery without weakening the loopback target; example.com transport smoke passed",
  "W8-A3L complete after hostile correction review: imported-Stylo computed values feed bounded Rust Flexbox layout and normal-desktop fixtures at 1366x768 and 1920x1080",
  "W8-A4T complete: Flex container backgrounds and borders enter the existing validated WebRender display-list path",
]
integration_evidence = [
  "docs/parity/site-compatibility.toml defines the controlled-fixture, example.com, DuckDuckGo, Google, and YouTube ladder and same-machine Firefox ESR comparisons",
  "Automated networking remains deterministic and local by default; public-site access is explicit opt-in",
  "Normal websites and YouTube remain blocked until transport, DOM/task/script, dynamic style/layout, page input, storage, media, and isolation gates close",
]

[[wave8.work_item]]
id = "W8-A2R"
owner = "agent-2-js-wasm"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = [
  "js/brimstone/src/js/runtime/browser_script.rs",
  "js/brimstone/src/js/runtime/bytecode/vm.rs",
  "js/brimstone/src/js/runtime/context.rs",
  "js/brimstone/src/js/runtime/gc/heap.rs",
  "js/brimstone/src/js/runtime/mod.rs",
  "js/brimstone/src/js/runtime/tasks.rs",
  "docs/handoffs/W8-A2R-brimstone-browser-script-admission.md",
]
acceptance = [
  "Owner-thread lifetime-branded initial-realm classic-script admission returns only pointer-free outcomes and reports",
  "Source, metadata, opcode, managed-allocation, recursion, job, deadline, and external-interrupt limits fail closed while preserving exact context reuse",
  "Primary script outcome and explicit bounded microtask checkpoint remain separate for browser-observable ordering",
  "Product JIT dispatch stays false and raw lifetime/GC, DOM binding, task-loop, loader, origin, and untrusted-content gaps remain explicit NO-GO gates",
]
evidence = [
  "Independent corrective-delta review returned bounded GO with no remaining finding",
  "Corrected gates passed 44 default, 134 baseline-JIT, 144 combined-stress, and 10 focused ASan/LSan tests",
  "Terminal browser-admission poison and fail-closed allocation cleanup regressions pass while product JIT dispatch remains disabled",
  "docs/handoffs/W8-A2R-brimstone-browser-script-admission.md",
]

[[wave8.work_item]]
id = "W8-A5"
owner = "agent-5-network-security-storage"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = [
  "netwerk/rust/wild_buzzard_net",
  "docs/handoffs/W8-A5-general-web-transport.md",
]
acceptance = [
  "A distinct credential-free general-web target supports HTTP and HTTPS hostnames without weakening numeric LoopbackTarget policy",
  "System-configured DNS and every resolved address pass explicit policy before connect",
  "rustls validates server identity against bundled roots with no invalid-certificate bypass",
  "Deterministic local TLS/DNS/limit/cancellation tests pass and broader proxy, HTTP/2/3, Fetch, cookie, cache, revocation, and enterprise-root gaps remain explicit",
]
evidence = [
  "61 deterministic tests passed with zero failures; one public-network test remains ignored by default",
  "Independent connector-delta security review returned GO with no high- or medium-severity finding; its low documentation finding was corrected",
  "Opt-in https://example.com/ returned HTTP 200 over authenticated TLS 1.3 with HTTP/1.1 ALPN",
  "docs/handoffs/W8-A5-general-web-transport.md",
]

[[wave8.work_item]]
id = "W8-A3L"
owner = "agent-3-dom-style-layout"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = [
  "layout",
  "servo/components/wild_buzzard_stylo_adapter",
  "docs/handoffs/W8-A3L-normal-page-flex-layout.md",
]
acceptance = [
  "Imported Stylo remains the sole CSS parser/cascade/computed-value engine",
  "Bounded Rust Flexbox layout covers high-leverage normal-page row/column sizing, alignment, distribution, and gap behavior without site-specific hacks",
  "Deterministic 1366x768 and 1920x1080 search-page fixtures record structural and geometry evidence",
  "Unsupported formatting contexts fail honestly and normal-site/Firefox parity remains false",
]
evidence = [
  "Independent first review returned NO-GO for post-flex wrapped-text cross sizing; the corrected delta then received GO",
  "Corrected exact fixture resolves a 20px item to three text lines and 30px item/container heights",
  "Final owned matrices passed 7+17 layout tests and 1+28 Stylo-adapter tests with strict lints, release, and rustdoc gates",
  "docs/handoffs/W8-A3L-normal-page-flex-layout.md",
]

[[wave8.work_item]]
id = "W8-A4T"
owner = "main-orchestrator-graphics-integration"
model = "gpt-5.6-sol"
reasoning_effort = "high"
state = "complete"
write_paths = [
  "gfx/wild_buzzard_renderer/src/compiler.rs",
  "gfx/wild_buzzard_renderer/tests/scene_compiler.rs",
  "gfx/wild_buzzard_renderer/README.md",
  "docs/handoffs/W8-A4T-flex-decoration-paint.md",
]
acceptance = [
  "Flex element boxes use the same preflight-counted background and border path as block/inline boxes",
  "Anonymous, text, and line-break boxes remain excluded",
  "A live layout-to-scene regression proves exact Flex source-box decoration identity",
]
evidence = [
  "Two unit and 23 renderer integration tests passed with zero failures",
  "Strict all-target Clippy, release, warning-denied rustdoc, rustfmt, and whitespace gates passed",
  "docs/handoffs/W8-A4T-flex-decoration-paint.md",
]

[active_wave]
id = "W9"
name = "real desktop navigation and scripted-document vertical"
state = "active"
integration_goal = "Turn the reviewed W8 transport, layout, renderer, Brimstone, and Rust shell foundations into an honest normal-desktop browsing path: load public HTTP(S) documents through HTML/DOM/Stylo/layout/render, fix standards-derived blockers, connect the result to visible chrome, and compare reproducible 1366x768 and 1920x1080 behavior with Firefox ESR."
tasks = [
  "W9-A6I complete: the separate general-web capability now drives the real headless and presentation page pipelines at normal desktop viewports while preserving cancellation, deadlines, generation freshness, and fail-closed redirects",
  "W9-A6J complete: ordinary Rust-shell startup, address submission, back, forward, and reload retain the general-web capability; deterministic product smoke remains numeric-loopback-only",
  "W9-A3M complete: Stylo preserves automatic physical margins, CSS2 LTR block-width layout resolves them after constraints, and RTL fails typed before box or fragment publication",
  "W9-A2S complete for the contained gate: bounded Brimstone classic-script and checkpoint phases mutate exact-version rooted Rust DOM tasks without enabling untrusted-page execution",
  "W9-A6K complete after corrective hostile rereview: bounded top-level GET redirects publish exact final URL and transport-security evidence through browser history/address identity without overwriting an owned address draft or IME composition",
  "W9-A3N complete after corrective independent rereview: exact Firefox root/body canvas-background selection, containment, sealed provenance, and one bottom-of-viewport solid paint are implemented without site-specific behavior",
  "W9-A3O complete after independent documentation rereview: imported Stylo's collapsed-nowrap pair remains typed through layout, collapses ASCII whitespace without soft wrapping, preserves mixed Normal/Nowrap boundary opportunities, and still honors explicit line breaks",
  "W9-A5L complete after corrective hostile rereview: one bounded final-response policy-input envelope is inseparable from its exact initial document and navigation commitment without fetching or enforcing external resources",
  "W9-A5M complete after corrective hostile rereview: a capability-free bounded CSP style-policy parser/matcher intersects enforcing policies, evaluates report-only policies without granting permission, and retains redacted transactional failure evidence without activating fetch or style enforcement",
  "Post-nowrap desktop probes complete: DuckDuckGo produces a sparse visible static frame at both required viewports and Google advances to the exact typed display:inline-block blocker; all 4.30 GB of transient probe state was removed",
  "W9-A3Q complete after two corrective hostile review cycles: fixed-width display:inline-block is a distinct bounded atomic inline item with an independent existing block-flow interior, nearest-common-ancestor CSS Text wrapping, checked atom geometry, and renderer decorations",
  "The clean post-inline-block Google probe advances from display projection to the deliberate auto-width shrink-to-fit stop at DOM slot 24 in both desktop viewports; its 4,274,342,968 bytes of transient source and target state were removed",
  "W9-A3R complete after corrective hostile review and post-commit orchestrator closure: bounded auto-width inline-block shrink-to-fit passes the focused and broad owner matrices",
  "W9-A6L complete: a retained generic HTTPS comparison probe refreshes the exact visible Firefox ESR153 YouTube baseline and records Wild Buzzard's two zero-content compositor frames without claiming parity",
  "Next: connect the reviewed CSP matcher to the document-owned external stylesheet security/admission sequence without broadening its conservative source subset, while the parser-blocking script, VM rendering, automation, and continuously staffed JavaScript lanes advance in parallel",
]
integration_evidence = [
  "Deterministic 1366x768 system-DNS HTTP and 1920x1080 authenticated local-TLS documents produce visible frames through the real page stack",
  "The opt-in https://example.com/ probe produces visible 1366x768 and 1920x1080 frames; centered body geometry matches Firefox ESR while generic canvas-background and typography differences remain recorded",
  "The contained rooted Brimstone/DOM bridge passes default, combined-feature, forced-GC, fixed-heap OOM, strict Clippy, rustdoc, and ASan/LSan gates; independent hostile review returned GO while untrusted-page dispatch remains disabled",
  "The redirect/final-identity lane passed a second independent hostile review after dirty-editor, non-2xx admission, trust-boundary documentation, and redirect-error coverage corrections; network, engine, and UI matrices pass with exact one-shot final metadata",
  "The corrected canvas lane passed independent rereview with no findings: exact computed image-list and containment facts, private identity/style/ancestry/version provenance, source suppression, resource accounting, and first-item ordering are covered across both desktop viewports",
  "The collapsed-nowrap lane passed independent code review and a focused documentation rereview: 8 layout unit plus 31 integration tests and 1 Stylo-adapter unit plus 38 integration tests pass, with the unsupported Unicode line-break boundary documented rather than approximated",
  "The final-response envelope passed corrective hostile rereview after replacing an infallible boxed-URL duplicate with immutable shared commitment storage and correcting test attribution; 82 retained binaries pass with one public-network smoke deliberately ignored",
  "The CSP style-policy matcher passed corrective hostile rereview after report-only transaction, oversized-nonce, quote-token, IPv6, and integration-fixture corrections; its canonical matrix passed 110 tests with one public-network smoke deliberately ignored plus strict check, Clippy, release, and rustdoc gates",
  "Commit 40e46fa3f0af0ca2cf79fff251ac994c77f28b60 adds the pure CSP matcher; its exact 8,115,178,672-byte external task root was deleted after push, leaving no W9-A5M build or test artifact",
  "The clean post-nowrap probe records exact DDG frame/evidence counts and Google's inline-block stop at 1366x768 and 1920x1080 in docs/parity/site-compatibility.toml without claiming proper-site or interaction parity",
  "The inline-block lane passes 8+40 layout, 1+41 Stylo-adapter, and 2+33 renderer tests plus strict lint, release, rustdoc, format, and whitespace gates; independent rereview returned GO after checked atom-wrap geometry closed the final finding",
  "The shrink-to-fit lane passes 8+56 layout and 1+41 Stylo-adapter tests plus locked checks, strict owner Clippy, release, warning-denied rustdoc, exact-path rustfmt, and whitespace gates in the Data-drive Debian 13/Rust 1.90 container; no fresh public-site parity is claimed",
  "The exact Mozilla Firefox ESR153.0.1 archive checksum matches its published SHA256SUMS; visible WebDriver navigation, consent interaction, video play/pause, measured frame-time progression, and screenshots prove the live YouTube reference while Wild Buzzard reports zero non-clear pixels at 1366x768 and 1920x1080",
  "A fresh 27,502,104-byte release shell built under the Data-drive Podman target and opens on the physical Wayland host; its exact in-guest copy on Ubuntu 24.04 fails safely at the recorded SoftwareRasterizer gate, proving that CPU presentation remains open",
  "The Debian 13 test VM is restored from its experimental Venus/EGL-headless definition to ordinary virtio display, boots an active 1280x800 Wayland desktop, and reproduces the current release's safe SoftwareRasterizer stop with zero submitted frames",
  "The exact 4,499,656,917-byte W9-A3Q task tree was removed after commit c4fa4663034deffa42d13a15e22b29b1d8c1865c was pushed; docs/parity/site-compatibility.toml records the artifact-free post-inline Google stop without a frame or parity claim",
  "Firefox ESR 153.0.1 reference captures exist for DuckDuckGo, Google consent, and YouTube consent at 1366x768 and 1920x1080; the full-HD captures wait for populated document states, and no corresponding Wild Buzzard verification is claimed",
  "docs/parity/example-comparison.toml records exact source revision, artifact hashes, geometry, and known differences",
  "docs/parity/site-compatibility.toml remains the source of truth for the controlled-fixture, example.com, DuckDuckGo, Google, and YouTube ladder and same-machine Firefox ESR comparisons",
  "Normal websites, YouTube, Firefox parity, and AppImage completion remain false until the recorded script, dynamic document, input, storage, media, isolation, shell, and comparison gates close",
]

[[active_wave.work_item]]
id = "W9-A6I"
owner = "agent-6-browser-product"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = [
  "browser/wild_buzzard_engine",
  "docs/handoffs/W9-A6I-general-navigation.md",
]
acceptance = [
  "A separately authorized general-web request feeds bounded DNS, TCP, authenticated TLS, HTTP response delivery, HTML, DOM, imported Stylo, layout, text, and headless or presentation output on the existing worker",
  "One absolute deadline, cancellation token, and navigation generation remain authoritative through transport and every later synchronous stage",
  "Deterministic 1366x768 HTTP and 1920x1080 TLS fixtures yield visible frames; capability mismatch, stale result, timeout, and redirect behavior fail closed",
  "Public-site probes report the first real standards blocker without stripping author content or claiming browser-shell/normal-site parity",
]
evidence = [
  "Five deterministic general-navigation tests passed and one explicit opt-in public probe remains ignored by default",
  "The public example.com probe reaches style translation and identifies automatic horizontal margins as the exact next blocker",
  "Full owner matrix passed 61 tests with zero failures and one ignored public-network test, plus formatting, strict lints, release, and rustdoc gates",
  "docs/handoffs/W9-A6I-general-navigation.md",
]

[[active_wave.work_item]]
id = "W9-A3M"
owner = "agent-3-dom-style-layout"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = [
  "layout",
  "servo/components/wild_buzzard_stylo_adapter",
  "docs/handoffs/W9-A3M-block-auto-margins.md",
]
acceptance = [
  "Imported Stylo automatic horizontal margins remain typed through the adapter rather than being discarded",
  "CSS2 block-width resolution handles one or two automatic horizontal margins, negative residual space, over-constraint, and vertical auto margins without site-specific logic",
  "Generic 1366x768 and 1920x1080 fixtures cover centered viewport-relative blocks and unsupported contexts fail typed",
  "The opt-in public example.com frame probe is rerun after integration and either passes or records the next exact generic blocker",
]
evidence = [
  "Independent first review returned NO-GO because inherited CSS direction was dropped; the corrected typed-direction delta received GO with no findings",
  "Layout passes 8 unit and 23 integration tests; the Stylo adapter passes 1 unit and 34 integration tests, plus strict Clippy, release, and warning-denied rustdoc",
  "A clean source-commit worktree passes all deterministic general-navigation tests and the opt-in public example test at both desktop viewports",
  "docs/handoffs/W9-A3M-block-auto-margins.md",
  "docs/parity/example-comparison.toml",
]

[[active_wave.work_item]]
id = "W9-A6J"
owner = "main-orchestrator-browser-integration"
model = "gpt-5.6-sol"
reasoning_effort = "high"
state = "complete"
write_paths = [
  "browser/wild_buzzard_engine/src/lib.rs",
  "browser/wild_buzzard_ui",
  "browser/wild_buzzard_shell",
  "docs/handoffs/W9-A6J-general-web-shell.md",
]
acceptance = [
  "The ordinary product constructs matching general-web presentation workers and requests while the deterministic smoke stays numeric-loopback-only",
  "Startup URL, address submission, back, forward, and reload cannot silently change network authority",
  "Bundled Web PKI roots remain the only ordinary product trust source and HTTPS chrome remains unverified until exact security metadata is published",
  "UI and shell tests, strict Clippy, release, rustdoc, formatting, and whitespace gates pass without claiming a public visible frame",
]
evidence = [
  "UI matrices pass 38 unit, 31 browser-session, 3 concrete-port, and 2 compile-fail doctests",
  "Shell matrix passes 34 unit tests; focused proofs cover six general-web session operations and ordinary-versus-smoke worker selection",
  "Strict all-target Clippy, release shell build, warning-denied rustdoc, exact rustfmt, and whitespace gates pass",
  "docs/handoffs/W9-A6J-general-web-shell.md",
]

[[active_wave.work_item]]
id = "W9-A2S"
owner = "agent-2-js-wasm"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = [
  "js/brimstone/src/js/runtime/browser_host.rs",
  "js/brimstone/src/js/runtime/browser_script.rs",
  "js/brimstone/src/js/runtime/context.rs",
  "js/brimstone/src/js/runtime/mod.rs",
  "dom",
  "docs/handoffs/W9-A2S-rooted-dom-task-bridge.md",
]

acceptance = [
  "Browser-owned bounded tasks enter the exact lifetime-branded Brimstone realm and expose only minimal rooted DOM operations",
  "Script output produces an exact-version bounded ScriptMutationBatch with browser-observable script-error and microtask ordering",
  "Moving-GC, stale task, cancellation, panic, OOM, and poison cases fail closed without leaking raw managed pointers",
  "Product dispatch for untrusted pages and the Brimstone JIT remains false until the security and conformance gates pass",
]
evidence = [
  "Default Brimstone workspace, combined baseline_jit/alloc_error/gc_stress_test/handle_stats, and root DOM matrices pass with zero failures",
  "ASan/LSan passes 40 core tests plus the bridge unit and five integration tests; combined tests record zero native-JIT entries",
  "Independent hostile review returned GO after reentry, token-field, queued-job, cancellation-precedence, and pre-execution task-validation corrections",
  "docs/handoffs/W9-A2S-rooted-dom-task-bridge.md",
]

[[active_wave.work_item]]
id = "W9-A6K"
owner = "agent-6-browser-product + agent-5-network-security-storage"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = [
  "netwerk/rust/wild_buzzard_net/src/target.rs",
  "netwerk/rust/wild_buzzard_net/src/general/tests.rs",
  "browser/wild_buzzard_engine",
  "browser/wild_buzzard_ui",
  "docs/handoffs/W9-A6K-redirect-final-identity.md",
]
acceptance = [
  "Top-level GET navigation follows 301, 302, 303, 307, and 308 under one ten-hop bound, absolute deadline, cancellation token, and navigation generation",
  "Browser identity retains normalized final fragments while every transport request is fragment-free; malformed, looping, credentialed, non-HTTP, non-UTF-8, and oversized redirect targets fail without document publication",
  "Exact final URL, redirect count, final connection security, and sticky HTTPS downgrade evidence transfer once for the exact navigation and update the exact history slot",
  "A current dirty address draft or active IME preedit survives commitment and Escape reverts to the new final URL; hostile non-2xx commit events fail before phase, history, or frame success",
]
evidence = [
  "The first independent review returned NO-GO for editor-state loss, non-2xx admission, an overclaimed authenticity boundary, redirect-error coverage, stale documentation, and incomplete inventory; all six were corrected",
  "The corrective independent rereview returned GO with no remaining finding across the exact 17-path freeze",
  "Locked/offline matrices passed 61 network tests with one public ignore, 67 engine tests with one public ignore, and 82 UI tests including two compile-fail doctests, plus strict Clippy, rustdoc, rustfmt, and isolation checks",
  "docs/handoffs/W9-A6K-redirect-final-identity.md",
]

[[active_wave.work_item]]
id = "W9-A3N"
owner = "agent-3-dom-style-layout + agent-4-graphics-media"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = [
  "layout",
  "gfx/wild_buzzard_renderer",
  "servo/components/style/properties/longhands.toml",
  "servo/components/wild_buzzard_style_platform",
  "servo/components/wild_buzzard_stylo_adapter",
  "docs/handoffs/W9-A3N-canvas-background.md",
]

acceptance = [
  "Firefox ESR's exact color-alpha plus one-none image-list predicate selects root precedence before canonical HTML-body fallback",
  "Effective root or body containment blocks fallback while a meaningful contained root remains the canvas source",
  "The exact DocumentVersion and private root/body/wrapper identity, style, and ancestry facts are validated before suppression or scene construction",
  "One source background is suppressed across all fragments and one exact-viewport canvas primitive is counted and emitted first at 1366x768 and 1920x1080",
  "Images and gradients block incorrect body fallback without claiming that their payloads are painted",
]
evidence = [
  "The first independent review returned NO-GO for incomplete computed background/containment facts and forgeable source provenance; both boundaries were redesigned and corrected",
  "The corrective rereview returned GO with no findings and directly reran 28 layout, 32 renderer, 37 Stylo-adapter, and 2 style-platform integration/unit binaries",
  "Owner gates passed locked/offline metadata, exact rustfmt, strict all-target Clippy, release builds, and warning-denied rustdoc on Linux x86-64",
  "All 4.65 GB of Cargo output and the 12 MB Stylo build environment were removed after integration; the external build root is empty",
  "docs/handoffs/W9-A3N-canvas-background.md",
]

[[active_wave.work_item]]
id = "W9-A3O"
owner = "agent-3-dom-style-layout"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = [
  "layout",
  "servo/components/wild_buzzard_stylo_adapter",
  "docs/handoffs/W9-A3O-collapsed-nowrap.md",
]

acceptance = [
  "Stylo's exact Collapse/Nowrap computed pair maps to a distinct collapsed-nowrap layout value while Preserve/Nowrap remains preformatted",
  "Repeated ASCII CSS whitespace collapses across nested inline boxes without adding a soft nowrap break, while an explicit br still forces a line",
  "Normal-owned and nowrap-owned pending spaces retain their distinct boundary-break eligibility without inventing a break for direct adjacency",
  "Generic 1366x768 and 1920x1080 adapter fixtures pass without hostname, content-pattern, tiny-window, or viewport-threshold production branches",
]
evidence = [
  "Independent code review returned GO with no behavioral finding; a documentation-only NO-GO for incomplete external-artifact accounting was corrected and the focused rereview returned GO",
  "Layout passes 8 unit and 31 integration tests; the Stylo adapter passes 1 unit and 38 integration tests, plus strict Clippy, release, warning-denied rustdoc, rustfmt, and whitespace gates",
  "The ASCII boundary behavior is derived from Firefox ESR reftests and focused CSS Text WPT inputs; ideographic Unicode line breaking remains explicitly unsupported",
  "The exact 4,013,592,591-byte external review directory was removed immediately after integration; no W9-A3O build or test artifact remains",
  "docs/handoffs/W9-A3O-collapsed-nowrap.md",
]

[[active_wave.work_item]]
id = "W9-A3Q"
owner = "agent-3-dom-style-layout + agent-4-graphics-media"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = [
  "layout",
  "servo/components/wild_buzzard_stylo_adapter",
  "gfx/wild_buzzard_renderer",
  "docs/handoffs/W9-A3Q-inline-block.md",
]
acceptance = [
  "Imported Stylo display:inline-block remains distinct through layout and renderer ownership rather than being approximated as inline or block",
  "A definite-width atom participates in the outer inline formatting context while its children use the existing bounded block/flex/inline interior; automatic margins resolve to zero",
  "Whitespace and no-space atomic boundaries use the nearest common inline ancestor required by the pinned CSS Text cases, with explicit line breaks still authoritative",
  "Box count, inline work, growing-prefix work, fragment aggregation, allocation, width, vertical, cursor, and publication arithmetic fail typed before unchecked work or saturation",
  "Both desktop Google probes clear display projection and stop at the honest auto-width shrink-to-fit boundary without a frame or parity claim",
]
evidence = [
  "Two corrective hostile-review cycles closed nearest-common-ancestor, quadratic-work, vertical-coordinate, and atom-triggered cursor-saturation findings; final independent rereview returned GO with no findings",
  "Layout passes 8 unit plus 40 integration tests, the Stylo adapter 1 plus 41, and the renderer 2 plus 33, together with locked/offline check, strict Clippy, release, warning-denied rustdoc, rustfmt, and whitespace gates",
  "The detached 204,286,430-byte probe source and 4,070,056,538-byte probe target were deleted immediately after both viewport results; the complete 4,499,656,917-byte task tree was deleted after integration",
  "docs/handoffs/W9-A3Q-inline-block.md",
  "docs/parity/site-compatibility.toml",
]

[[active_wave.work_item]]
id = "W9-A3R"
owner = "agent-3-dom-style-layout + main-orchestrator"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = [
  "layout",
  "servo/components/wild_buzzard_stylo_adapter",
  "docs/handoffs/W9-A3R-inline-block-shrink-to-fit.md",
]
acceptance = [
  "Auto-width non-replaced inline-blocks use the checked CSS2 preferred-minimum/available/preferred clamp while definite-width atoms retain the prior path",
  "Normal, nowrap, pre, atomic, block, and supported flex contributions remain distinct under one bounded cached traversal with typed stops for unsupported contributions",
  "Cyclic percentages, signed margins, box sizing, min/max precedence, work limits, and arithmetic overflow follow the recorded Firefox ESR and standards-derived behavior",
  "Generic 1366x768 and 1920x1080 fixtures prove exact shrink-to-fit geometry without hostname, page-content, or viewport-threshold production branches",
]
evidence = [
  "Independent review first returned NO-GO for signed atomic contributions, cyclic percentage-min behavior, and unmodeled wbr; corrections received GO with no remaining finding",
  "Post-commit orchestrator closure passed 8+56 layout and 1+41 Stylo-adapter tests, locked all-target checks, strict owner Clippy, release builds, warning-denied rustdoc, exact-path rustfmt, and whitespace gates",
  "All container layers, Cargo home/target, Python support, and generated outputs stayed under /run/media/user/Data/Repositories/wildbuzzardbuilds on the Data drive",
  "No public Google rerun or fresh site/frame parity is claimed by this gate",
  "docs/handoffs/W9-A3R-inline-block-shrink-to-fit.md",
]

[[active_wave.work_item]]
id = "W9-A6L"
owner = "main-orchestrator"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = [
  "browser/wild_buzzard_engine/tests/general_navigation.rs",
  "browser/wild_buzzard_engine/tests/static_pipeline.rs",
  "browser/wild_buzzard_engine/README.md",
  "docs/parity/site-compatibility.toml",
  "docs/handoffs/W9-A6L-live-public-parity-probe.md",
]
acceptance = [
  "One exact caller-supplied HTTPS URL traverses the normal bounded product DNS/connection policy and both desktop viewports without hostname, content, selector, or viewport-threshold implementation branches",
  "Optional pixels remain in an external Data-drive capture directory and the probe reports zero-content frames honestly rather than converting a successful pipeline call into a parity claim",
  "The exact official Firefox ESR153 binary runs visibly under WebDriver, publishes a BiDi endpoint, captures YouTube before and after a real consent interaction, and records scale/viewport differences",
  "A fresh release binary is built on the Data drive, opened on the physical host, transferred byte-identically into an ordinary Ubuntu VM, and records the current CPU-rendering failure gate",
]
evidence = [
  "The complete engine workspace passes 39 library and 71 integration tests after replacing one stale rasterizer-coordinate assertion with a bounded declared-color glyph-contribution check; no production renderer or pixel changed",
  "Five deterministic general-navigation tests and the retained example.com public probe pass after public and fixture network limits are separated",
  "The generic YouTube probe reaches 1366x768 and 1920x1080 frames with exactly zero non-clear pixels at each viewport",
  "Firefox ESR153.0.1 archive, visible consent/shell captures, WebDriver interaction, and artifact hashes are recorded in docs/parity/site-compatibility.toml",
  "The same live Firefox session decodes the 320x240 public Me at the zoo stream, advances currentTime by 4.009229 seconds after a WebDriver Play click, and returns to paused state after a second click; the exact JSON trace and three screenshots are retained and hashed",
  "A second Firefox WebDriver trace verifies an exact ten-second paused seek plus mute/unmute, captions on/off, fullscreen entry, and Escape exit state transitions with three additional hashed screenshots",
  "Raw aligned screenshot comparison records 99.9663517% and 99.9829765% changed pixels at the two required viewports, with a hashed JSON report and difference heatmaps; the poor scores are an explicit blank-frame baseline, not a parity threshold",
  "Commit de5ceb5275ce6875ab69c558dce4412967c1af23 records and pushes the initial live-reference, blank-Wild-Buzzard, host, VM, and deterministic probe checkpoint",
  "The 27,502,104-byte release binary has SHA-256 0a702b356ba9295a6911754883e749a9509a8f750bc6e05530c6006861e44e6d; Ubuntu 24.04 verifies the same hash and fails safely at SoftwareRasterizer",
  "Debian 13 now boots a standard virtio/Wayland desktop after exact XML backup and recovery; its matching release copy reaches the same checked SoftwareRasterizer gate, providing a clean CPU-fallback acceptance target",
  "docs/handoffs/W9-A6L-live-public-parity-probe.md",
]

[[active_wave.work_item]]
id = "W9-A5L"
owner = "agent-5-network-security-storage + agent-6-browser-product"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = [
  "browser/wild_buzzard_engine",
  "docs/handoffs/W9-A5L-document-policy-envelope.md",
]
acceptance = [
  "Only the exact final admitted top-level response contributes ordered bounded CSP, report-only CSP, Referrer-Policy, Content-Type, and privacy-safe Set-Cookie observations",
  "The private envelope is inseparable from the exact initial DocumentVersion and NavigationCommitMetadata, survives unrelated mutation/rerender/context movement, and cannot replace a prior page on failure",
  "Every family and aggregate byte/count/work bound is checked with fallible retained allocation, while sensitive CSP and cookie inputs are absent from Debug, Display, errors, pipeline evidence, and UI output",
  "This prerequisite changes no policy admission, cookie state, subresource request, or desktop frame pixel",
]
evidence = [
  "The first independent review returned NO-GO for an infallible final-URL clone and inaccurate handoff test categorization; immutable Arc-backed URL ownership plus a pointer-sharing regression and corrected evidence received GO",
  "The complete engine matrix passes 82 tests with one deliberate public-network ignore; strict Clippy, check, release, warning-denied rustdoc, rustfmt, and whitespace gates pass",
  "Policy-bearing and plain documents produce pixel-identical visible frames at 1366x768 and 1920x1080",
  "The exact 8,052,964,922-byte external review tree was removed immediately after integration; no W9-A5L build artifact remains",
  "docs/handoffs/W9-A5L-document-policy-envelope.md",
]

[[active_wave.work_item]]
id = "W9-A5M"
owner = "agent-5-network-security-storage + main-orchestrator"
model = "gpt-5.6-sol"
reasoning_effort = "ultra"
state = "complete"
write_paths = [
  "browser/wild_buzzard_engine/src/style_policy.rs",
  "browser/wild_buzzard_engine/src/lib.rs",
  "browser/wild_buzzard_engine/tests/style_policy.rs",
  "browser/wild_buzzard_engine/README.md",
  "docs/handoffs/W9-A5M-csp-style-policy.md",
]
acceptance = [
  "Separate enforcing and report-only CSP fields parse under aggregate member/source and per-policy byte/directive/work bounds without concatenation or truncation",
  "Every enforcing style policy must admit an operation while report-only policies can only increment a privacy-safe would-block count; any report-only parse or merge failure rolls back that complete transaction",
  "The admitted HTTP(S) scheme, host, port, wildcard, self, nonce, and unsafe-inline subset follows the pinned ESR behavior recorded in the handoff; paths, hashes, bracketed IPv6 sources, and other unsupported expressions remain typed nonmatching evidence",
  "The parser is capability-free and disconnected from fetching and style application, so policy-bearing and plain documents retain byte-identical frames at 1366x768 and 1920x1080",
]
evidence = [
  "Independent hostile rereview returned GO after transactional report-only assembly, over-limit nonce continuation, one-byte quote safety, fail-closed IPv6 handling, and the final report-only fixture were corrected",
  "The clean detached-base matrix passed 19 internal policy tests, 9 style-policy integration tests, and 110 complete engine workspace tests with zero failures and one intentional public-network ignore",
  "Locked Linux x86-64 all-target check, strict no-dependency Clippy, release workspace build, warning-denied no-dependency rustdoc, exact rustfmt, and whitespace gates passed",
  "The exact 8,115,178,672-byte external task root was removed immediately after commit 40e46fa3f0af0ca2cf79fff251ac994c77f28b60 was pushed; no W9-A5M artifact remains",
  "docs/handoffs/W9-A5M-csp-style-policy.md",
]
