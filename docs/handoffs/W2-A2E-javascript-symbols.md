# W2-A2E JavaScript Symbol and own-key handoff

- Task: W2-A2E identity-bearing Symbols, unified PropertyKeys, tracing, ordered own-key reflection, and rooted embedding operations
- Owner: Agent 2 — JavaScript/WebAssembly; corrected, integrated, and independently reviewed by the main orchestrator
- Status: Complete for the bounded Wave 2 subset; this is not complete ECMAScript, SpiderMonkey, WebAssembly, DOM binding, browser, or Firefox parity
- Firefox commit and source paths: ESR153 `c19b7e89270787889495688244ec6ee8e79288a1`; Symbol allocation and built-ins, property-key representation, native-object storage, ordinary own-key enumeration, Array keys, and relevant history revisions are recorded in `js/README.md`
- Firefox test paths: focused Test262 and SpiderMonkey Symbol uniqueness, description, construction, property-reflection, own-key order, large-index, and property-enumeration cases are listed in `js/README.md`
- Wild Buzzard paths changed: `js/src/ast.rs`, `js/src/heap.rs`, `js/src/lexer.rs`, `js/src/parser.rs`, `js/src/runtime.rs`, `js/tests/symbols.rs`, and `js/README.md`
- Contract added or changed: a private generation-checked live-Symbol arena owns optional exact `JsString` descriptions; `PropertyKey` distinguishes owned strings from Symbol identities; ordered property storage preserves redefinition and delete/reinsert rules; Symbol keys are checked strong GC edges; rooted host APIs create and describe Symbols and use them as keys without exposing private identities; reflection materializes bounded Arrays from one shared own-key source
- Tests run and results: 100 tests passed, including 10 focused Symbol integration tests and three Symbol/ordering heap unit tests; exact-file Rust 2024 formatting, package all-target/all-feature check, strict Clippy with `-D warnings`, locked tests, release build, warning-denied rustdoc, and `git diff --check` passed for `x86_64-unknown-linux-gnu` in external build directories
- Parity evidence: every successful `Symbol()` has fresh identity; absent and empty descriptions remain distinct and lone surrogates survive; implicit Symbol string/number conversion fails; `new Symbol` fails; direct Symbol keys participate in descriptors, reads, writes, own checks, deletion, and collection; indices sort numerically before insertion-ordered strings and Symbols; Array `length` appears between indexed and named keys; redefinition is stable while deletion/reinsertion appends within its category
- Known behavioral differences: the global Symbol registry, well-known Symbols, iterator protocols, boxed Symbol wrappers, Symbol-aware `Object.prototype.toString`, object ToPrimitive/ToPropertyKey, primitive-target Object reflection, proxies and other exotics, and the special unresolvable-identifier behavior of `typeof` remain absent; `%Symbol.prototype%` is correctly an ordinary object without `[[SymbolData]]` and is correctly rejected by the Symbol receiver methods
- Unsafe or FFI introduced: None; the crate forbids unsafe code, adds no native engine wrapper, and adds no dependency
- Licenses and provenance: MPL-2.0 first-party Rust informed by the pinned ESR153 implementation, tests, and history; no Firefox source was copied and `firefox/` is not a build or runtime input
- Provider or network implications: None; the crate performs no I/O and introduces no endpoint
- Blocked on: Nothing for continued JavaScript/WebAssembly work; cross-realm shared identities must be designed before global-registry or well-known-Symbol storage is finalized
- Recommended next action: add a pinned metadata-aware Test262 runner and continue the language/runtime surface without pausing the JavaScript lane; separately begin a reused-Rust WebAssembly validation boundary before execution and GC integration
