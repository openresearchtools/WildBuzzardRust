# W2-A3T immutable Stylo-to-layout handoff

- Task: W2-A3T concrete immutable DOM adapter, real Stylo element resolution, and revision-checked static layout projection
- Owner: Agent 3 — DOM, style, and layout; corrected and independently validated by the main orchestrator
- Status: Complete for the bounded static snapshot-to-computed-style-to-horizontal-layout slice; this is not live invalidation, full CSS/layout, an end-to-end page, or Firefox parity
- Firefox commit and source paths: ESR153 `c19b7e89270787889495688244ec6ee8e79288a1`; imported Stylo source/provenance and inspected selector, root-rem, link, sizing, writing-mode, CSSOM, and WPT references are recorded in `docs/upstream-components.toml` and `servo/WILDBUZZARD_ADAPTATION.md`
- Wild Buzzard paths changed: `servo/components/wild_buzzard_stylo_adapter`, the wild_buzzard-only typeless sharing-cache slot in `servo/components/style/sharing/mod.rs`, nested `servo` manifests/adaptation notes, root `layout`, provenance/status documents, and this handoff
- Contract added or changed: bounded immutable `AdapterSnapshot`; safe adapter/index element handles; exact-revision `SelectorStateSnapshot`; `ComputedStyloSnapshot`; exact document/revision `ComputedStyleSnapshot`; supported edge/color/text/sizing/box-sizing/writing-mode projection; style-snapshot layout entry points and explicit unsupported-writing-mode errors
- Tests run and results: 20 concrete adapter tests, 15 root layout tests, 38 focused Stylo/selector tests, and 64 complete nested-workspace tests passed; three imported documentation examples remain deliberately ignored; root and adapter strict rustdoc passed, while full imported-workspace rustdoc used the four pre-recorded upstream-debt allowances
- Parity evidence: the imported `Stylesheet`, `Stylist`, selector matcher, `StyleResolverForElement`, generated cascade, and `MatchMethods::finish_restyle` paths compute primary element styles; root completion updates descendant `rem`; selected computed sizes produce checked root-layout geometry
- Known behavioral differences: immutable sequential snapshots only; no live/parallel invalidation, shadow trees, pseudo-element output, generated content, visited styles, quirks mode, animations, complete UA/user sheets, real device/theme/font metrics, or loader-to-pixel integration; intrinsic/anchor/complex sizing, auto margins, vertical flow, and many formatting contexts fail explicitly or remain absent
- Unsafe or FFI introduced: the first-party adapter contains no unsafe block, raw-pointer dereference, or transmute; five trait-mandated `unsafe fn` signatures have safe bodies and documented exclusive-preparation invariants; the imported Stylo core and its typeless sharing cache retain their previously recorded audited unsafe internals
- Licenses and provenance: the adapter and layout changes are MPL-2.0; imported Stylo sources retain their recorded MPL/MIT/Apache notices and exact ESR153 provenance
- Provider or network implications: none; external `@import` is prohibited, the only built-in URL uses `.invalid`, and no telemetry, provider service, credential, or runtime network operation was introduced
- Blocked on: no blocker to continued static integration; parity requires real font/device/UA data, layout-to-shaped-text ownership, broad CSS/WPT/reftests, and the complete loader/render product pipeline
- Recommended next action: connect this exact-revision style snapshot and the same immutable shaped-text object through layout and the existing renderer, then expand property/layout coverage only behind focused WPT/reftest and fail-closed gates
